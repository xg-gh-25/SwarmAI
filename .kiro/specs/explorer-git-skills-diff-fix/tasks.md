# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - Directory Flat-Path Git Status & Skill Projection
  - **CRITICAL**: This test MUST FAIL on unfixed code — failure confirms the bugs exist
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior — it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bugs exist
  - **Scoped PBT Approach**: Scope the property to concrete failing cases for reproducibility
  - Test file: `backend/tests/test_property_explorer_git_bugfix.py`
  - Include module-level docstring per SwarmAI coding standards
  - Use `fsWrite` + `fsAppend` for file creation, never heredoc
  - **Sub-properties to test:**
    - **1a — `_build_tree()` direct-match**: Create a `git_status` dict with a flat directory entry (e.g., `{"dir/subdir": "untracked"}`) where no child paths exist with that prefix. Call `_build_tree()` and assert the directory node receives `git_status == "untracked"`. On unfixed code, `_build_tree()` only does prefix scan (`rel_path + "/"`), so the directory gets `None`.
    - **1b — Symlink vs copytree**: Call `ProjectionLayer.project_skills()` and assert the resulting entries under `.claude/skills/` are NOT symlinks (i.e., `is_symlink() == False` and `is_dir() == True`). On unfixed code, entries are symlinks (mode 120000), so git tracks the pointer not the content.
    - **1c — Parent directory propagation**: With a flat-path entry for `.claude/skills/s_code-review` (no child prefix entries), assert the parent `.claude/skills/` directory also gets a git status. On unfixed code, parent inherits nothing because the child has no status.
  - Property generator: for all `(dir_name, git_status_code)` where `dir_name` is a valid directory path and `git_status_code` in `{"untracked", "modified", "added"}`, assert `_build_tree()` assigns the status to the directory node when `dir_name` is a direct key in `git_status`
  - Run test on UNFIXED code — expect FAILURE (this confirms the bugs exist)
  - Document counterexamples found (e.g., `_build_tree()` returns `dir_status = None` for flat-path entries)
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Regular File/Directory Git Status & Modal Behavior
  - **IMPORTANT**: Follow observation-first methodology
  - Test file: `backend/tests/test_property_explorer_preservation.py`
  - Include module-level docstring per SwarmAI coding standards
  - Use `fsWrite` + `fsAppend` for file creation, never heredoc
  - **Observation-first steps:**
    - Observe: `_build_tree()` with `git_status = {"dir/file.txt": "modified"}` correctly assigns `"modified"` to `file.txt` node on unfixed code
    - Observe: `_build_tree()` with `git_status = {"dir/sub/file.txt": "modified"}` correctly assigns `"modified"` to `dir/sub/` directory via prefix scan on unfixed code
    - Observe: `ProjectionLayer.project_skills()` respects tier precedence (built-in always projected, user/plugin gated) on unfixed code
    - Observe: `_get_git_status()` correctly parses all porcelain status codes on unfixed code
  - **Preservation properties to test:**
    - **2a — Regular file git status**: For all `(file_path, status)` pairs in `git_status` where `file_path` is a file (not a directory), `_build_tree()` assigns the correct status to the file node. This uses the existing `rel_path in git_status` check for file nodes, which is NOT being changed.
    - **2b — Child-prefix directory status**: For all directories where child paths exist in `git_status` with the `prefix + "/"` pattern, `_build_tree()` assigns `"modified"` to the directory node via the existing prefix scan. Generate random trees with only child-prefix entries (no flat-path directory entries) and verify behavior matches unfixed code.
    - **2c — Skill tier precedence**: For all combinations of `(allowed_skills, allow_all)`, `project_skills()` projects built-in skills unconditionally and gates user/plugin skills correctly. Extends existing tests in `test_property_skill_symlinks.py`.
    - **2d — Git status code parsing**: For all valid porcelain status codes, `_get_git_status()` maps them to the correct status strings.
  - Verify all tests PASS on UNFIXED code
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 3. Fix for directory flat-path git status, symlink-to-copytree migration, committed file endpoint, FileEditorModal diff, skill read-only enforcement, and gitStatus bridging

  - [x] 3.1 Implement `_build_tree()` direct-match check (Fix 1)
    - In `backend/routers/workspace_api.py`, function `_build_tree()` (~line 325)
    - Before the prefix scan loop, add: `if rel_path in git_status: dir_status = git_status[rel_path]`
    - The prefix scan still runs after and can upgrade `dir_status` to `"modified"` if children also have status
    - This handles symlink flat-path entries like `?? .claude/skills/s_code-review` that have no trailing slash
    - Parent directory propagation (Bug 3) is automatically resolved because the child now has status, so the parent's prefix scan finds it
    - _Bug_Condition: `input.node.type == "directory" AND input.node.path IN git_status AND NOT any child prefix match`_
    - _Expected_Behavior: `dir_status == git_status[rel_path]` for direct-match entries_
    - _Preservation: Prefix scan still runs for child-derived status; regular files unchanged_
    - _Requirements: 2.1, 2.3_

  - [x] 3.2 Implement `ProjectionLayer` symlink→copytree migration (Fix 2)
    - In `backend/core/projection_layer.py`, function `project_skills()`
    - Add `import shutil` at the top of the module
    - Replace `link_path.symlink_to(skill_path.resolve())` with `shutil.copytree(str(skill_path.resolve()), str(link_path), dirs_exist_ok=True)`
    - Update existing-symlink check: replace `link_path.is_symlink()` with `link_path.exists()` for the idempotency check
    - For "target changed" comparison: use `shutil.rmtree()` + `copytree()` (app recreates on every launch, clean re-copy is acceptable)
    - Rename `_cleanup_stale_symlinks()` to `_cleanup_stale_entries()` — handle both symlinks (legacy) and real directories: `shutil.rmtree()` for dirs, `unlink()` for symlinks
    - Update `_validate_symlink_target()` — rename to `_validate_skill_source()` for clarity (no longer symlink-specific)
    - Update module-level docstring to reflect copytree instead of symlinks
    - _Bug_Condition: `isSymlink(skill_path) AND gitMode == 120000`_
    - _Expected_Behavior: `NOT isSymlink(link_path) AND is_dir(link_path) AND git tracks content`_
    - _Preservation: Tier precedence unchanged; stale entry cleanup still works; validation still checks tier roots_
    - _Requirements: 2.2_

  - [x] 3.3 Implement `GET /workspace/file/committed` endpoint (Fix 3 + Fix 9)
    - In `backend/routers/workspace_api.py`, add new endpoint
    - Accept `path` query parameter (relative to workspace root)
    - Validate path with `_validate_relative_path()`
    - Run `subprocess.run(["git", "show", f"HEAD:{path}"], capture_output=True, timeout=10)` in the workspace directory
    - Decode stdout manually (not `text=True`) to catch `UnicodeDecodeError` for binary files → return HTTP 400
    - Return `{"content": <committed_content>}` on success
    - Return `{"content": ""}` if file is untracked (git returns non-zero exit code)
    - Return 404 if the file doesn't exist at all in the workspace
    - _Bug_Condition: `fileEditorState.gitStatus != null AND originalContent == content`_
    - _Expected_Behavior: `committed content returned for tracked files, empty string for untracked`_
    - _Preservation: No existing endpoints affected; new endpoint only_
    - _Requirements: 2.4_

  - [x] 3.4 Update skill file handling after copytree (Fix 6)
    - In `backend/routers/workspace_api.py`, function `get_workspace_file()`
    - Rename `is_skill_symlink` variable to `is_skill_file`
    - Remove or simplify the symlink escape hatch (lines ~468-479) that allows reading files outside workspace root — after copytree, skill files are real files inside the workspace
    - The `readonly: true` return already works via `path.startswith(".claude/skills/")` — no change needed there
    - _Bug_Condition: `path STARTS_WITH ".claude/skills/" AND is_skill_symlink escape hatch`_
    - _Expected_Behavior: `skill files readable as regular files inside workspace, readonly: true preserved`_
    - _Preservation: Regular file reads unchanged; readonly flag logic unchanged_
    - _Requirements: 2.5_

  - [x] 3.5 Block writes to skill files (Fix 7)
    - In `backend/routers/workspace_api.py`, function `put_workspace_file()`
    - Add guard at the top: `if path.startswith(".claude/skills/") or path.startswith(".claude\\skills\\"): raise HTTPException(status_code=403, detail="Skill files are read-only")`
    - Defense-in-depth alongside frontend readonly mode
    - _Bug_Condition: `PUT request to path under .claude/skills/`_
    - _Expected_Behavior: `HTTP 403 returned, file not modified`_
    - _Preservation: Regular file writes unchanged_
    - _Requirements: 2.5_

  - [x] 3.6 Bridge `gitStatus` in double-click flow (Fix 8)
    - In `desktop/src/components/workspace-explorer/FileTreeNode.tsx`: add `gitStatus?: GitStatus` to `FileTreeItem` interface
    - In `desktop/src/components/workspace-explorer/toFileTreeItem.ts`: add `gitStatus: node.gitStatus` to the mapping object
    - In `desktop/src/components/layout/ThreeColumnLayout.tsx`: pass `file.gitStatus` to `openFileEditor(file, file.gitStatus)` in the double-click handler
    - _Bug_Condition: `node.gitStatus != null AND toFileTreeItem(node).gitStatus == undefined`_
    - _Expected_Behavior: `gitStatus preserved through TreeNode → FileTreeItem → openFileEditor`_
    - _Preservation: Files without gitStatus unaffected (field is optional)_
    - _Requirements: 2.6_

  - [x] 3.7 Fetch committed version in `openFileEditor()` (Fix 4)
    - In `desktop/src/components/layout/ThreeColumnLayout.tsx`, function `openFileEditor()`
    - When `gitStatus` is truthy, make second API call to `GET /workspace/file/committed?path=<path>`
    - Add `committedContent?: string` to `fileEditorState`
    - On success: set `committedContent` to the response content
    - On failure (untracked file): set `committedContent` to `""`
    - Pass `committedContent` to `FileEditorModal` as a new prop
    - _Bug_Condition: `gitStatus != null AND no committed version fetched`_
    - _Expected_Behavior: `committedContent populated from git show HEAD:<path>`_
    - _Preservation: Files without gitStatus skip the second API call_
    - _Requirements: 2.4, 2.6_

  - [x] 3.8 Use committed version in `FileEditorModal` (Fix 5)
    - In `desktop/src/components/common/FileEditorModal.tsx`
    - Add `committedContent?: string` to `FileEditorModalProps`
    - In the `useEffect` that resets state when modal opens: `setOriginalContent(committedContent ?? initialContent)`
    - When `committedContent` is provided and differs from `initialContent` (disk content), `isDirty` becomes true, enabling "Show Changes"
    - The diff shows committed version vs current disk content
    - _Bug_Condition: `gitStatus != null AND originalContent == content (both disk content)`_
    - _Expected_Behavior: `originalContent == committedContent, isDirty == (committedContent != diskContent)`_
    - _Preservation: Files without committedContent fall back to initialContent (existing behavior)_
    - _Requirements: 2.4_

  - [x] 3.9 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Directory Flat-Path Git Status & Skill Projection
    - **IMPORTANT**: Re-run the SAME test from task 1 — do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run `cd backend && pytest tests/test_property_explorer_git_bugfix.py -v`
    - **EXPECTED OUTCOME**: Test PASSES (confirms bugs are fixed)
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.10 Verify preservation tests still pass
    - **Property 2: Preservation** - Regular File/Directory Git Status & Modal Behavior
    - **IMPORTANT**: Re-run the SAME tests from task 2 — do NOT write new tests
    - Run `cd backend && pytest tests/test_property_explorer_preservation.py -v`
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all preservation tests still pass after fix (no regressions)

- [x] 4. Checkpoint — Ensure all tests pass
  - Run full backend test suite: `cd backend && pytest`
  - Run full frontend test suite: `cd desktop && npm test -- --run`
  - Ensure existing tests in `backend/tests/test_property_skill_symlinks.py` still pass (update if needed for copytree migration)
  - Ensure all new property tests pass
  - Ask the user if questions arise
