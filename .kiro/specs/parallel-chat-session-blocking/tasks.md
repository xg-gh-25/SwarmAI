# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - Parallel New Sessions Blocked by Shared Agent Lock
  - **CRITICAL**: This test MUST FAIL on unfixed code — failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior — it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate two concurrent new sessions (both `session_id=None`, `app_session_id=None`, same `agent_id`) collide on the same lock key
  - **Scoped PBT Approach**: Scope the property to the concrete failing case: `app_session_id=None`, `session_id=None`, `agent_id="default"` — two concurrent calls should each get a unique lock key (UUID) and neither should receive `SESSION_BUSY`
  - **Test file**: `backend/tests/test_property_parallel_session_fault.py`
  - **Setup**: Create an `AgentManager` instance, mock `_configure_claude_environment`, `_build_options`, `_ClaudeClientWrapper`, and `session_manager.store_session` (same pattern as `test_property_auth_error_fault.py`)
  - **Bug condition from design**: `isBugCondition(input)` — `app_session_id IS None AND session_id IS None AND lock_key == agent_id AND _session_locks[lock_key].locked() == True`
  - **Test logic**: Launch two concurrent `_execute_on_session()` calls via `asyncio.gather()` with `session_id=None`, `app_session_id=None`, `agent_id="default"`. The mock client should yield a slow async response (e.g., `asyncio.sleep(0.5)` + init message + result message). Collect events from both calls. Assert that NEITHER call yields a `SESSION_BUSY` error event — both should proceed independently.
  - **Expected on UNFIXED code**: Test FAILS — the second concurrent call receives `SESSION_BUSY` because both compute `lock_key = "default"` (the `agent_id` fallback)
  - **Expected on FIXED code**: Test PASSES — each call gets a unique UUID lock key
  - Use Hypothesis with `max_examples=3` to generate random `agent_id` strings, confirming the bug is not specific to `"default"`
  - Run test on UNFIXED code, document the counterexample (e.g., "Second concurrent new session with agent_id='default' received SESSION_BUSY")
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 2.1, 2.2_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Double-Send Protection and Lock Cleanup for Resumed Sessions
  - **IMPORTANT**: Follow observation-first methodology — observe behavior on UNFIXED code first
  - **Test file**: `backend/tests/test_property_parallel_session_preservation.py`
  - **Setup**: Same mock pattern as task 1 — `AgentManager` instance with mocked environment, options, client wrapper, and session manager
  - **Observation phase** (run on UNFIXED code to establish baseline):
    - Observe: Two concurrent requests with same `session_id="abc-123"` → second gets `SESSION_BUSY` ✓
    - Observe: Two concurrent requests with same `app_session_id="tab-456"` → second gets `SESSION_BUSY` ✓
    - Observe: `_cleanup_session("abc-123")` removes lock from `self._session_locks` ✓
    - Observe: Session with error (`had_error=True`) cleans up from `_active_sessions` ✓
  - **Property-based tests** (from Preservation Requirements in design):
    - **Property 2a**: _For all_ `session_id` values that are not `None`, two concurrent `_execute_on_session()` calls with the same `session_id` SHALL result in the second call yielding `SESSION_BUSY`. Use Hypothesis to generate random non-None `session_id` strings.
    - **Property 2b**: _For all_ `app_session_id` values that are not `None`, two concurrent `_execute_on_session()` calls with the same `app_session_id` SHALL result in the second call yielding `SESSION_BUSY`. Use Hypothesis to generate random non-None `app_session_id` strings.
    - **Property 2c**: _For all_ session IDs, after `_cleanup_session(sid)` completes, `sid` is NOT in `self._session_locks` and NOT in `self._active_sessions`.
  - **Expected on UNFIXED code**: All preservation tests PASS (confirms baseline behavior to preserve)
  - **Expected on FIXED code**: All preservation tests still PASS (confirms no regressions)
  - Run tests on UNFIXED code, verify all pass
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 3. Fix for parallel new chat sessions blocked by shared agent lock

  - [x] 3.1 Implement ephemeral UUID lock key in `_execute_on_session()`
    - In `backend/core/agent_manager.py`, method `_execute_on_session()` (~line 1373)
    - Change `lock_key = app_session_id or session_id or agent_id` to `lock_key = app_session_id or session_id or str(uuid4())`
    - Add `is_ephemeral_lock = (app_session_id is None and session_id is None)` flag after lock_key computation
    - Add observability logging: `logger.info(f"Using ephemeral lock key {lock_key} for new session (agent={agent_id})")` when ephemeral, `logger.debug(f"Using stable lock key {lock_key} for session")` otherwise
    - Wrap the `async with session_lock:` block in a `try/finally` that pops the ephemeral key from `self._session_locks` when `is_ephemeral_lock` is True
    - `uuid4` is already imported at line 30 — no new import needed
    - _Bug_Condition: isBugCondition(input) where app_session_id IS None AND session_id IS None AND lock_key == agent_id_
    - _Expected_Behavior: Each new session gets a unique UUID lock key; neither blocks the other with SESSION_BUSY_
    - _Preservation: Resumed sessions (app_session_id or session_id not None) use the same lock key as before; double-send protection unchanged_
    - _Requirements: 1.1, 1.2, 2.1, 2.2, 2.4, 2.5_

  - [x] 3.2 Add concurrency guard to `run_skill_creator_conversation()`
    - In `backend/core/agent_manager.py`, method `run_skill_creator_conversation()` (~line 2507)
    - Add lock key computation: `lock_key = session_id or str(uuid4())`
    - Add `is_ephemeral_lock = (session_id is None)` flag
    - Get lock: `session_lock = self._get_session_lock(lock_key)`
    - Add `session_lock.locked()` check → yield `SESSION_BUSY` error (same pattern as `_execute_on_session`)
    - Wrap existing try/except block with `async with session_lock:`
    - Add `finally` block to pop ephemeral key from `self._session_locks`
    - Add observability logging matching the pattern from 3.1
    - _Bug_Condition: run_skill_creator_conversation has no concurrency guard at all_
    - _Expected_Behavior: Same-session concurrent requests get SESSION_BUSY; new sessions get unique UUID lock keys_
    - _Preservation: All existing skill creator behavior (client reuse, error handling, session storage) unchanged_
    - _Requirements: 1.3, 2.3, 2.4, 2.5_

  - [x] 3.3 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Parallel New Sessions Proceed Independently
    - **IMPORTANT**: Re-run the SAME test from task 1 — do NOT write a new test
    - The test from task 1 encodes the expected behavior (unique UUID lock keys, no SESSION_BUSY)
    - When this test passes, it confirms the expected behavior is satisfied
    - Run: `cd backend && pytest tests/test_property_parallel_session_fault.py -v`
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed — each new session gets a unique lock key)
    - _Requirements: 2.1, 2.2_

  - [x] 3.4 Verify preservation tests still pass
    - **Property 2: Preservation** - Double-Send Protection and Lock Cleanup for Resumed Sessions
    - **IMPORTANT**: Re-run the SAME tests from task 2 — do NOT write new tests
    - Run: `cd backend && pytest tests/test_property_parallel_session_preservation.py -v`
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions — double-send protection intact, lock cleanup works)
    - Confirm all preservation properties still hold after fix

- [x] 4. Checkpoint - Ensure all tests pass
  - Run full test suite: `cd backend && pytest`
  - Verify no regressions in existing tests
  - Verify `test_property_parallel_session_fault.py` passes (bug fixed)
  - Verify `test_property_parallel_session_preservation.py` passes (no regressions)
  - Verify `test_property_auth_error_fault.py` and `test_property_session_persistence_fault.py` still pass (unrelated tests unaffected)
  - Ensure `self._session_locks` does not grow unboundedly — ephemeral keys cleaned up after execution
  - Ask the user if questions arise
