
### 2026-07-19: recall-suite — the first quantified recall number since the vector leg was removed; honestly 0.71 (run_50fad0fb, goal)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What worked — we now have a NUMBER where there was none.** Since the vector leg was torn out (pure-filesystem keyword/FTS5, MOD03) recall ran on "感觉够用" with zero evidence. This shipped a mechanical, non-circular recall@K/MRR benchmark (`recall_suite.py` + a thin `eval_recall_at_k` evaluator in eval_runner) and produced the first real figure: **ddd_recall@5 = 0.71, MRR = 0.37** over 7 intent-first queries against the real pinned DDD corpus. **The number is honestly imperfect and that IS the value** — "what is the product vision" / "what are the non-goals" both MISS top-5 (short conceptual queries buried in a large multi-doc corpus), and MRR 0.37 shows even hits often aren't rank-1. A green rubber-stamp would have been worthless; a 0.71 that points at a real weakness is a baseline. This directly starts filling OT03 (E2E 自动验证链缺失) on the recall axis.

**What worked — Gate-1 (2 rounds) caught 4 structural errors BEFORE code, one fatal.** R1 BLOCKed 3: (a) per-domain hit shapes differ — session returns ONE `text[:500]` blob, codeintel returns symbol ids, only context_files+ddd have a stable `section` key, so a single `gold in top-K` check across all domains was impossible; (b) my plan ran against the LIVE workspace, but §24.1.1 (which I wrote) mandates a PINNED snapshot (else 0.7→0.6 = regression or content-drift? uninterpretable); (c) `compute_scores` is status-count-only and would DISCARD the metric. R2 BLOCKed a 4th: the ddd path via `recall_all` reads live disk — not pinnable — the fix was to call the lower-level `_ddd_section_scores_multi(query, docs_text)` which is pure over the passed corpus. Every one would have shipped a broken or meaningless benchmark. The scope correctly collapsed to "the 2 section-domains" (which is also exactly the domain the vector removal touched — context_files was the only vector path), deferring session/codeintel honestly.

**What worked — Gate-2 verified the number is HONEST, not circular.** The adversary's highest-value check: is gold picked by observing what recall returns (circular → always ~1.0)? No — gold is title-substring-matched while the ranker scores BODIES via BM25 (independent signals), and the 2 misses are the proof (a tuned set couldn't miss). It also independently reproduced 0.71/0.37 and confirmed the pinning claim (`_ddd_section_scores_multi` does zero file reads).

**What to watch:** (1) the number is **ddd-section-scoped**, tagged `ddd_recall_at_5` so it can't be misread as system-wide — session/codeintel recall is still unmeasured (deferred run). (2) intent-first gold selection is an **unenforced honor-checkpoint** (the s_golden-case gate checks schema/dup/vacuity/privacy, NOT provenance) — a future author could tune gold to behavior; the mechanical body-vs-title independence is the only structural guard. (3) the seed corpus is pinned at RUN time (live DDD snapshot); a versioned regression baseline needs a frozen fixture (the deferred Run 4). Deploy: this is scripts-only (not compiled into the binary), so it runs as-is — no rebuild needed to USE it, though it's not yet wired into `eval-scheduled` (Run 4).

### 2026-07-18: §24 graphify steal-list — 3 code-intel adoptions in one goal run; Gate-0/1 collapsed the scope, Gate-2 cleaned the tail (run_dd13fb03, goal)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What worked — Gate 0 + Gate 1 (2 rounds) shrank "build 3 features" to "build ~1.5" BEFORE any code, by tracing what already existed.** The requirement was "adopt §24.2 topology / §24.3 skipped-ledger / §24.4 ERROR-fallback." A fresh Gate-0 skeptic + code-trace found: **24.3 was ~90% already built** (`parse_repo_with_coverage` has a mature `coverage_holes` ledger: unknown-ext/gitignored/unreadable/failed/dead-lang/oversized → `doc[coverage_ledger]`) — only extensionless build files (Makefile) leaked silently; **24.4 was one focused gap** (`parse_file_with_status:1371` extracted symbols from a partial error-tree then DISCARDED them). Only **24.2 was greenfield**. Had I not traced, I'd have rebuilt a coverage subsystem that exists (C038 authorship-trap in reverse). The lesson: **when adopting an external design, the highest-value first move is `grep`+read your OWN code — the borrowed tool's problem framing is not your gap inventory.**

**What worked — Gate 1 caught a CATEGORY ERROR that would have shipped a useless feature.** My first plan computed god-nodes as degree over `module_edges` — but those are 2-level dir pairs, so that's god-*modules*, not god-*symbols* (a hub symbol like `MessageStore.append`, deg 3317, is invisible at module granularity). And surprising-connections at module level was **empirically dead**: only 8/372 module endpoints map to a domain (2%). Gate-1 BLOCKed both; I re-planned to symbol-level degree (own SQL) + a file→domain map derived from nested `business_rules[].anchor`+`issues[].file`+`steps→flows.domain_id` (32 files) with an **honest coverage stat** in the output. The reframe made surprising-connections real (file-level) instead of a dead module-level stub. **Gate value peaks exactly where felt-confidence is highest — I was sure module_edges was the right input.**

**What worked — Gate 2 found 2 real defects in the one NEW module** (last-writer-wins inverting a documented precedence; an unguarded None-sort that would fail-open-drop the whole analysis block). Both fixed + regression-tested in one convergence iteration. Per-stage review would have missed both (they live in cross-function seams).

**What to watch — the self-verify-the-skeptic discipline paid off (both directions).** Gate-1 round-2 BLOCKed on "business_rules/issues don't exist in the doc" — I verified against the real doc and found the skeptic had an INCOMPLETE read (they're nested under `domains[]`, not top-level; 32 files map, not 0). I folded the *correct* corrections and rejected the wrong objection with evidence, rather than blindly obeying OR blindly dismissing. R16b cuts both ways: a sub-agent's claim about my system is an input to verify, not a verdict to obey.

**Honest residue:** god-nodes surfaces high-degree TEST symbols (`_stage_record` in a test file) alongside prod hubs — accurate (tests do have high degree) but arguably noise for an "architecture" view; graphify includes them too. A test-file demotion (like the recall `_TEST_SYMBOL_RANK_PENALTY`) is a possible future refinement, not shipped (scope discipline). Deploy: 4 commits, R6 local Build+Tests green; binary rebuild+restart deferred to user (code-intel core is compiled in).

### 2026-07-18: code-intel module_edges confidence + god-node guard — Gate 0 overturned all 3 requirement premises (run_2392a203, full)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What worked — Gate 0 diagnose-before-build reframed a wrong requirement before a line of code.** The requirement (ported from studying graphify) assumed (1) module_edges were "black/white, no confidence", (2) import edges = EXTRACTED, (3) a god-node guard was needed. A code-trace at EVALUATE overturned **all three**: (1) `code_edges` ALREADY has `edge_type`+`confidence` columns (0.5/0.6/0.8/1.0); (2) the parser produces NO `imports` edges at all (only `calls`/`references`) — so "import=EXTRACTED" was a phantom; (3) the guard WAS real but the true gap was that `get_module_edges()` **discarded the existing confidence** (`SELECT source_id,target_id` only) AND let 5398 confidence=0.5 bare targets (`str`/`mkdir`/`get`) leak in as fake single-token modules — **520/597 (87%) of the live module skeleton was garbage**. A fresh-context Gate-0 skeptic SUPPORTED the reframe against source (and strengthened it: the N-candidate import-match branch in `resolve_bare_targets` is effectively dead since no imports edges exist). **Lesson: when adopting an external design, the FIRST move is to trace your OWN code — the borrowed tool's problem framing is not necessarily yours** (GUI101). The fix landed at the aggregation layer (drop ≤0.5, MAX-confidence rollup, EXTRACTED/INFERRED label), NOT by deleting DB rows — 0.5 edges are legit per-symbol data, garbage only as a module node (R25 right layer). Result: 597→131 module_edges, fake endpoints 520→51 (90% removed), Gate-2 clean on all 7 vectors.

**What worked — the MAX-vs-MIN aggregation question was settled by DATA, not debate.** Before choosing, I measured the live DB: of 827 raw module-pairs, 696 were all-bare(≤0.5) + 131 all-resolved(>0.5) + **0 MIXED**. Zero mixed pairs means MAX vs MIN is moot for today's data — but Gate-1 required a mixed-pair test anyway to LOCK the MAX choice for future data. Measuring the distribution before designing the semantics (O008) turned a "which is right?" argument into a non-question.

**What to watch — GUI85 recurred (and was caught + recovered):** to mutation-test the guard I `git checkout backend/core/code_intel/graph_store.py` to revert the temporary mutation — which ALSO nuked my real (unshipped) implementation. Caught immediately (tests went RED with the impl gone), re-applied from context. **The durable fix is NOT git-checkout for mutation reverts on a file holding unshipped edits** — apply+revert the mutation as an in-memory string swap on a copy, or stash only after committing the real work. This is the 2nd time this class bit; the lesson is on file (GUI85) but prose didn't stop it — the tell is "I'm about to `git checkout` a file I have uncommitted edits in."

**Known residue (tracked, out of scope):** ~51 endpoints remain single-token from EXTERNAL/stdlib qualified refs (`pathlib::Path` at confidence 1.0 → `_mod_of`→"pathlib"). These pass the guard (1.0, not ≤0.5) — a DISTINCT root cause needing a node-existence/external-symbol filter, not a confidence guard. Documented in commit, not hidden. Follow-up candidate if the module skeleton needs to be fully external-free.

### 2026-07-18: auth/startup robustness sweep — the win was DELETING 2 of 6 "findings", not just fixing 4 (run_b8285685, goal)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What worked — verify-then-exclude beat build-a-fix.** An E2E review handed me 6 findings. The highest-value move was proving **2 of them were NOT bugs** via live verification, and NOT writing code for them:
- **#1 "Anthropic-direct model-namespace is a dead path" (was flagged P0 by me the prior turn) — FALSE.** The `claude-api` skill's authoritative model catalog states `claude-opus-4-6`/`-4-8`/`sonnet-4-6` ARE the exact valid Anthropic API IDs ("do not append date suffixes"). A `anthropic_model_map` would be dead code (PIT70: defensive code guarding the normal case IS the bug). This was a **CLASS B self-inflicted false alarm** — I'd asserted "short name → 4xx" from how the string *looked*, without checking the authority. Loading the skill I invoked to *source the fix* is what falsified my own P0.
- **#4c "bedrock_model_map suffix inconsistency (4-8 no suffix, 4-6-v1 has one)" — FALSE.** A live `aws bedrock list-inference-profiles` (this box runs on Bedrock) confirmed `us.anthropic.claude-opus-4-8`, `...-4-6-v1`, AND `...sonnet-4-6` are ALL real profile IDs — the "inconsistency" is the real AWS catalog. R16b: observe (live probe) beat inferring from string shape. Normalizing the suffix would have BROKEN the `-v1` lookup.
- **The reusable lesson: when a review hands you N findings, the first pass is "which of these are real?" — verified by the authority (doc/live system), not by how plausible they feel. Excluding a non-bug is worth as much as fixing a real one, and building a fix for a non-bug is negative work (dead code + a broken real ID).**

**The 4 real fixes (all mutation-verified RED-on-revert):** #2 `get_auth_hint` sync `_probe_ada_details`/`_probe_aws_profiles` → `asyncio.to_thread` (a ~10s ada-CLI hang was blocking the whole event loop; the sibling `_probe_iam_instance_role` already used to_thread — pure symmetry gap). #3 `set_secret` read-modify-write got a cross-process lock. #4a `pollHealth` had NO wall-clock ceiling and `alive` resets its only counter → a flapping backend spun forever in Hive/browser (no Rust backstop there). #4b `_verify_bedrock`/`_verify_anthropic_api` leaked raw provider `str(e)` (ARN/account-id/request-id) into the client `error` field.

**Gate value this run:**
- **Gate-1 caught a Windows-boot crash before code:** my plan said `import fcntl` at module-top of `app_config_manager.py` — but that module loads on the Windows subprocess path (main.py:24→system.py:21), so top-level fcntl = fatal `ModuleNotFoundError` at boot. Fix: reuse the existing cross-platform `utils/file_lock.py` helper (fcntl/msvcrt) with a function-local import. **A convenience import in a boot-path module is a deployment-mode bug that no test on this machine would ever see.**
- **#3 lock mechanism correctness (Step-1.7 declaration paid off):** the lock MUST be on a *persistent sibling* `secrets.json.lock`, NOT on `secrets.json` — the RMW ends in `os.replace` which swaps the inode, so a lock on the file's fd guards the wrong inode after the swap. (Same inode-vs-path flock trap as run_edcfd0e5.)
- **Gate-2 caught test-theater in my own #4a test + a lying comment:** the `hasExceededStartupCeiling` *helper* was tested but its *wiring into pollHealth* was not (R28 — a wiring regression would pass all tests). Added a forced-execution render test (mutation-verified: disable the ceiling `if` → RED). Also fixed a comment that falsely claimed the health+readiness ceilings "share one budget, not doubled" (they're independent ~2× windows) — a lying comment is a legibility-decay bug (R7).
- **Non-theater test discipline held:** the Anthropic-leak RED test initially PASSED vacuously (I'd aliased `ConnectError = Exception`, catching the sentinel early) — caught it in the RED check, made ConnectError a distinct class so the sentinel reaches the `except Exception` leak branch. RED-that-passes is the signal to distrust the test, not the code.

### 2026-07-18: auth E2E findings resolved method-correctly — the leak unit tests couldn't see + a Gate-2-caught regression in my own fix (run_84df955d, goal)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What worked.** A 5-tracer E2E user-journey review (over LIVE source, not the prior session's conclusions per R16b) found the auth flow's main paths worked but 6 residual findings remained — none catchable by the existing unit suite because they live at the *cross-layer seam* (verify-error copy, hint↔frontend suggestion consistency, banner state-vs-wording). The goal run fixed all 6 with mutation-verified tests (16 new, every one RED-on-revert).

- **F1 (the target bug class) — `_verify_bedrock` hardcoded `ada credentials update` in BOTH the expired and invalid branches**, so an external SSO/bedrock_api_key user who failed a Bedrock verify was told to run an Amazon-internal command. It survived because there was **no method-aware test on the verify-error path** — the SSOT `auth_remediation.remediation_for()` existed and was used at the banner + spawn layers, but the verify layer was never wired to it. Fix = reuse the SSOT; `remediation_for(None)` → safe generic fallback (no ADA jargon).
- **F1b — the frontend only sent `auth_method` in the verify override for `bedrock_api_key`** (AuthConfigPanel.tsx:112), so F1's `config.get("auth_method")` was `None` for ada/sso at verify time → generic fallback, losing the `aws sso login` hint. The Gate-0 skeptic caught this: fixing F1 without threading the method from the UI would half-fix it. Now always sent.
- **F4 — `get_auth_hint` suggested `apikey` even on an internal machine** (has_api_key won before the context check), then the frontend snap-to-first silently discarded it → user landed on the wrong card. Root fix at the SOURCE (suggestion logic), not the React symptom. Added `detection_confidence` so F3 can make the internal/external toggle prominent only when detection actually defaulted.
- **F2 design fork (A vs B) — chose wording-only over a 4th `AuthStatus`.** A never-configured user saw "expired/refresh". The tempting fix was a new `missing` status threaded through the validator + 2 consumers + whitelist — but the skeptic showed **no consumer branches differently from `expired`** (both just block/banner), so a typed state buys nothing behaviorally and touches the one spawn-gating value (R25 wrong-layer). Fixed the *copy* in CredentialBanner using the `hasAdaDir/hasSsoCache/hasApiKey` flags the hint already returns.

**Lesson (meta) — two-sided, both load-bearing.** (1) **The bug class this whole effort targeted (Amazon-internal credential commands like `ada`/`mwinit` shown to non-Amazon users) had a survivor precisely where there was no test at the layer** — SSOT existing ≠ SSOT wired everywhere; grep every layer that surfaces remediation, not just the obvious one. (2) **My own F2 fix introduced a NEW HIGH regression** — the `configured = has_ada||has_sso||has_api_key` check breaks Hive, where those flags are force-false but the IAM instance role IS valid → a dead-end "No credentials configured" banner. **Gate-2 (fresh-context adversarial with the mandatory cross-path questions) caught it; I did not.** This is the authorship-trap in miniature: I was confident the F2 fix was clean because its own tests were green — the felt-confidence of a self-authored fix is indistinguishable from a correct one, so the adversarial gate's value is highest exactly on the code I'm surest of. Fixed in convergence iter 1 (iam_role/hive = always-configured) with a mutation-verified Hive test.
<!-- ref:0 | last:none | decay:active | source:manual -->
**What worked.** A goal run to "truly close the code-intel→recall→cultivation loop + close all owner known-problems" re-scoped hard against empirical ground truth (Gate-0 WRONG-FRAME→REFRAMED, Gate-1 BLOCK→RESHAPE): of 4 candidate DoD, **2 were real** and **2 were already-satisfied / non-problems** — proven by running the code, not reading it.

- **DoD1 (loop) — the real bug was the SIGNAL, not a missing consumer.** `detect_spec_details_staleness` used **file mtime** (code-intel.json mtime > spec.md mtime = stale). A reindex rewrites code-intel.json (mtime bumps) while PRESERVING identical `domains[]`, so it false-fired **all 8 SwarmAI specs stale** at rest. Fix = **domain CONTENT-hash**: the skill (`ai_ready_helpers._spec_content_hash`, the SINGLE definition) hashes a domain's *rendered skeleton* (covers domain+flows+steps — a flow/step change alters the spec, so a domain-dict-only hash would false-fresh); the exporter stamps `domains[].spec_hash` at write time (the one place all three are in hand); `freshness.py` only **reads+compares** the stamp vs the spec's `<!-- spec-hash: X -->` marker. No two-writer hash drift (Gate-1 F1b). Then `context_health_hook` creates a **Radar todo** (real consumer, reusing `escalation.create_radar_todo`, dedup on `spec_details_stale:<project>`) — NOT a sink-less event (GUI04). Regeneration stays LLM-in-agent (C046) — the todo is the operator trigger.
- **DoD2 (gitignore) — real code.** Parser now honors the target repo's `.gitignore` via ONE batched `git check-ignore --stdin` AFTER file-gathering (never a per-file fork), layered under the hardcoded `SKIP_DIRS` fast-path (the sole mechanism for non-git repos). Tracked files matching a pattern are never dropped (git check-ignore skips them by design). Ignored files RECORDED as `coverage_holes kind='gitignored'` (O030). Fail-open on non-git/git-error.
- **DoD3 (single-JSON full-load) — CLOSED, measured non-problem.** `recall_multi._score_domains` full-loads code-intel.json per recall query, but **measured 2.22ms/query @193KB** (timeit ×50); frontend reads the SQLite DB (`routers/code_intel.py`), not the JSON. Sharding/lazy-load is unjustified. **Re-open trigger: JSON >10MB AND domains[] present AND recall latency measurable.** Zero code.
- **DoD4 (multi-project fan-out) — CLOSED, already satisfied.** `code_intel_reindex.reindex_projects` already iterates ALL `Projects/*/code_intel.db`. Design §7.4 "fan-out" is *within-repo domain parallelism* (deferred perf), a different axis — not a loop gap. Zero code.

**Lesson (meta).** The M3 skeptic said "all 4 broken," quoting the design's **stale §8.9 RED baseline (2026-07-16)** as current state — the C038 trap (Runs 3/4/E shipped after it). Live re-probe corrected it. My own opening frame ("all already-satisfied") was equally wrong. The truth (2 real, 2 close) came only from RUNNING the read legs + measuring, per O008/R16b. Reaching to "add a consumer for the staleness signal" would have built a mechanism on top of a **broken signal** — the actual fix was making the signal correct first (C044-family: fix the thing, don't paper it).

**What to Watch For (deferred by judgment, run_fe26ed6c meta-review MED — NOT a bug today):** `context_health_hook._signal_stale_spec_details` runs on EVERY session-start over ALL `Projects/*/` and is NOT budget-gated — per project it `json.loads` the whole code-intel.json + globs + reads each `*.spec.md`. Cost is O(#projects × #specs), sub-second at today's project count (measured), but has no per-iteration deadline check → a latent unbounded session-start tail at large scale. **Deliberately NOT fixed now** (O008: don't optimize an imaginary problem; building a cache for a non-current bottleneck is the C042/C044 premature-mechanism trap this very run's EVALUATE guarded against). Trigger to fix: when #projects × #specs makes session-start latency measurable — then add a `(code-intel.json mtime + spec-dir mtime)` fingerprint skip or fold into the existing 25s cultivation budget. Also LOW: `_stamp_spec_hashes` imports the skill via sys.path — verified working in both the source-tree reindex job AND the PyInstaller bundle (`_internal/skills/` shipped by build-backend.sh:290), fail-open+logged if ever absent; optionally assert skill presence in `verify_build.py` so a build dropping `_internal/skills` is caught at build time.

### 2026-07-15: Security gate matched the wrong verb — release_publish_guard gated `gh release create` but the real publish is `gh release edit --draft=false` (run_900bb839, bugfix)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What Failed → Fixed:** `release_publish_guard` (the CI-green gate for GitHub Release publish) matched only `gh release create`. But since release went CI-driven, the actual publish action is `gh release edit --draft=false` (flip a CI-created draft to published) — a verb the gate never intercepted, so **the CI-green gate was silently bypassed for the real publish path** (observed live: the v1.25.0 flip ran unchecked). This is the P7 pattern — a gate whose *detection surface* drifted out from under the *thing it guards*. Fixed with `_is_release_publish` matching create OR edit-with-draft-false. **Two dialect-assumption failures caught by the adversarial sub-agent, both because it LIVE-PROBED gh 2.88.1 and I did not (PIT16 — a dialect trap must be live-probed before trapping it):** (1) my first regex matched only literal `false`, but gh's `--draft` is a boolean pflag whose Go `strconv.ParseBool` false-set includes `0`/`f` → `gh release edit --draft=0` published UNCHECKED = a real HIGH bypass I introduced *while fixing a bypass*; (2) my "fix" then over-corrected to include `no`/`n` as false-tokens — but ParseBool does NOT accept those (gh rejects `--draft=no`), so those regex branches + tests were dead weight against commands gh aborts. Correct false-set verified live = exactly `{false, f, 0}` (case-insensitive); `--draft false` space form is a hard gh arg error (never publishes, not matched). **Durable lesson: when writing a string-shape guard against an external tool's flag semantics, PROBE the real tool for the exact accepted-value set — do not reason from the one spelling you have in mind (PIT14 whack-a-mole + PIT16 live-probe). The 2-pass adversarial with a live-gh probe is what turned two confident-but-wrong dialect models into the verified {false,f,0}.** Residual (documented LOW, unchanged): a raw `gh api graphql updateRelease draft:false` bypasses both regexes — same contrived-wrapper class as `bash -c`.

### 2026-07-15: Skill runbook drifted from CI reality — s_swarm-release Stage 5/6/7 described a manual flow CI had replaced (run_01fd142f, docs)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What to Watch For:** `s_swarm-release/INSTRUCTIONS.md` told the agent to locally `npm run tauri build` a DMG and `gh release create` it — but the repo had since CI-ized release: pushing the `v*` tag triggers `.github/workflows/release.yml`, which builds macOS/Windows/Hive on GitHub runners and creates a **draft** release (`softprops/action-gh-release@v2`, `draft:true`); the ONLY manual step is flipping draft→published (`gh release edit --draft=false`). During the v1.25.0 release I nearly walked the stale manual path (would have uploaded a single-platform local DMG over a complete CI draft). **Caught by observing `release.yml` + the live workflow run, NOT by the doc** — the doc was confidently wrong. **Rule: a skill runbook that wraps an external toolchain (CI, build system, cloud) is a HYPOTHESIS about that toolchain, not truth — when the runbook and the actual workflow file disagree, the workflow file wins, and the runbook is the bug (O026 skill-docs-drift family, C038 stale-doc-misleads family).** Two structural findings this surfaced: (1) `release_publish_guard` hook gates only the legacy `gh release create` verb, NOT the `gh release edit --draft=false` flip the flow now uses → the CI-green gate is currently runbook-enforced, not code-enforced for publish (owned by a follow-up bugfix run — the gate should intercept the real publish verb). (2) `release.yml` publish `if:` ships macOS OR Hive even if Windows build fails → 7b must treat Windows assets as best-effort WARN, not hard-FAIL. Adversarial sub-agent caught 2 residual-drift + 1 misleading-"CODE-ENFORCED" claim in my own rewrite before commit — the rewrite of a drift-fix is itself drift-prone.

### 2026-07-12: CSS refactor that moves active-styling class→CSS silently breaks class-literal test assertions (run_70ad47bc, trivial)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What to Watch For:** A visual refactor that moves active-state styling OUT of inline Tailwind class literals (`bg-[var(--color-primary)]/15`, `ring-1`) INTO a CSS class rule (`.nav-btn[aria-pressed=true]` reading a `--ac` group-color var) silently breaks any unit/property test that asserted the OLD className literals. My BUILD-stage same-file R7 scan **missed 4** such stale assertions in `ThreeColumnLayout.property.test.tsx`; only the TEST stage caught them (9 red → fixed → 80/80). **Rule:** when styling moves class→CSS, `grep ALL test files for the removed class literal BEFORE running the suite`, not after. The correct post-refactor assertion is `aria-pressed` + the stable class name — because **jsdom does not apply CSS attribute-selector rules**, so active-vs-inactive *visual* distinction is no longer unit-testable (needs a Playwright/visual test if it matters). Companion win: Gate-2 adversarial earned its keep on a "trivial" CSS-only change — caught a lying comment (claimed "footer keeps amber"; code correctly falls back to `--color-primary` accent) = R7 docstring-drift describing the mockup default, not shipped behavior.

### 2026-07-01: MILESTONE — "reliability + self-eval" 三周攻坚告一段落 (v1.23.0 shipped)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What:** 三周 refactor 的主线不是功能堆料，而是把系统从"看着好了"逼到"真的好了" —— 结构性根治了三类"骗过自己"的失败模式，每一类的共性都是"表面绿/看着活但实际错"（authorship-trap / 假信号的家族）：
- **(1) Session resume 死循环 & 首发 zombie** — 簇A WS1：串行化 `_client_io` 单 SDK 通道 + bounded `tool_call_leak` recovery（1st leak 注入纠正前缀+resume，2nd clean cold restart，杜绝自我强化重试）；DEAD-resume race：`_last_turn_clean` fail-closed guard（污染的 warm 子进程复用前 recycle）+ DEAD-state send-time recovery（撞 dead 自恢复，不干等 60s lifecycle loop）+ `_cleanup_dead` TOCTOU guard。
- **(2) Streaming spinner 假信号 / render-freeze (OT01)** — backend-authoritative spinner（后端状态权威驱动）+ render-source 层根治（33 次复发全在 layer 1-9 打转，真 defect 在 React render-source）+ hard-cap backstop clock（churn-immune force-clear 边界）。
- **(3) Golden case 假绿 (Eval)** — `gate_refs` anti-drift（每个 ref 必须解析到活内容否则 case fail）+ `hard_delete_cases`（物理删除、防复活）+ `validate-corpus` sweep（堵住 resting-corpus 漂移洞）+ judge-infra coverage-collapse 告警（errored case 不再冒充 clean pass）。
- 附带治理升级：AGENT R30（context files = 认知器官：verify-before-quote / touch-it-fix-it）+ s_persist Step-0 admission gate。

**Lesson — 这条主线值得，不要下次把同类"看着好了"问题当新问题重趟。** 三类失败表面无关（后端并发 / 前端渲染 / eval 数据），根因同族：**"表面信号为真"被当成"实际行为为真"**，这正是 SOUL P2（Done = 试着弄坏它并失败）+ CLASS-A authorship-trap 的系统级体现。修法也同族：把"真信号"变成结构性权威（backend-authoritative spinner / gate_refs 必解析 / 有界恢复而非无限重试），而非加容灾兜底。

**Lesson — 三周投入在最后一米就回本了一次。** 发版当天 CI red（`chatStreamingState` 的 `toEqual` 漏了 OT01 新增的 `postDisconnectFlushing` 字段 —— 生产代码对、测试陈旧），被三周养成的纪律当场拦住：本地跑证明它是真 red 而非旧失败 → 根因定位为测试脱节而非代码 bug → adversarial CONFIRMED-CLEAN（还额外发现该字段在 TS type 里是必填，坐实测试才是陈旧方）→ 修复+commit 才放行。没有"本地证明/adversarial 必过/观测不推断"这套，这个 red 很可能带着上线。

### 2026-06-30: E2E disconnect verification chain — Gate-2 found a false-green INSIDE the anti-false-green test (run_91170d96, full)
<!-- ref:0 | last:none | decay:active | source:pipeline -->
**What:** Built the missing test layer behind ~33 OT01 recurrences (87 disconnect/reconcile commits in 7d): no test ever injected a REAL mid-stream SSE drop — all coverage mocked the boundary or fabricated the failure (`errorHandler(new Error(...))`), and the fix I shipped the same day (run_27485b25) had only mocked tests. Built two real-transport tests: frontend drives the REAL `consumeSSEStream` against a no-[DONE] fetch stream (mutation-proven); backend `smoke_e2e._check_disconnect_recovery` drops a real stream mid-flight and asserts recovery.
**Lesson — a "passes" assertion on a recovery check is worthless unless it can DISTINGUISH recovery from a non-event; enumerate every way it can be true.** Gate-1 caught the first false-green (`streaming==false` alone is true for a healthy recovery AND a phantom-never-streamed AND a vanished session). I fixed it to 4 teeth. Then Gate-2 caught a SURVIVING false-green in MY teeth: `persisted = len(messages)>0` — but the USER row is persisted BEFORE the model call (`session_router.py:1600-1615`), so the tooth meant to prove "recovery flushed the answer" passes even on zero assistant output. The durable rule: for any "X was persisted/recovered" assertion, ask "what is written REGARDLESS of X?" and exclude it — assert the SPECIFIC artifact X produces (an assistant row with non-empty text), never a count that a pre-existing row satisfies.
**Lesson — the gate's value is highest on its own author, and a test-building task is the strongest proof.** I built this entire chain TO catch false-greens, and still shipped one into Gate-2. The felt-confidence in "my 4-assertion teeth are rigorous" was indistinguishable from the confidence before the 3-assertion version Gate-1 rejected. Containment held only because the adversarial gate re-derived each tooth against the real backend — not because I got more careful. CLASS-A authorship trap, test-layer variant: "I wrote a rigorous test" ≡ "the test is rigorous" is exactly as false as "I wrote it" ≡ "it works".
**Lesson — a fixed sleep before asserting an async-flushed result is a flaky-red waiting to happen.** Recovery is two-phase (sync STREAMING→IDLE + background assistant-flush); `sleep(3)` raced the flush once the assertion required the assistant row. Poll-with-retry against the real signal, never a fixed wait (O030 family: a timeout is disaster-recovery, not a normal-path control).
**Disclosed (Gate-2 LOW, OUT OF SCOPE):** the pre-existing section-6 `state_clean` (smoke_e2e.py:542-560) still defaults absent→False (the exact false-green my new check corrected) — a pre-existing latent weakness, filed not silently fixed.

### 2026-06-30: Mid-stream SSE drop — the bug was an ASYMMETRIC recovery path, not a missing one (run_27485b25, bugfix)
<!-- ref:0 | last:none | decay:active | source:pipeline -->
**What:** A chat tab's streaming "entirely stopped" (no spinner, no content); the answer only appeared on the user's NEXT send (pulled from DB). Original frame: "frontend has NO per-stream auto-reconnect." Gate-0 skeptic REFUTED that frame by code-trace: the frontend has rich connection-phase reconnect (createErrorHandler/retryStreamFn/backoff) AND a heal-grace error path that correctly hands off to the reconcile loop. The real gap was narrow: the MID-STREAM onDisconnect path (createDisconnectHandler:3978) was the ASYMMETRIC EXCEPTION — its 30s timeout unconditionally cleared the spinner + did a ONE-SHOT DB pull, never setting _postDisconnectUncertain, so the reconcile loop's recovery (the heal-grace path's owner) never engaged. Fix: make the disconnect timeout symmetric with heal-grace (reuse healGraceExpiryVerdict; still-working → hand off to reconcile loop; done → original clear+pull).
**Lesson — when a recovery symptom recurs, ask "is recovery MISSING, or ASYMMETRIC across sibling paths?" before building new machinery.** The original frame ("no reconnect") would have built a duplicate always-on reconnect loop — directly colliding with the existing connection-phase machinery. The truth was one of N sibling paths (onDisconnect vs onError/heal-grace) lacked the handoff the others had. R25 in its purest form: the fix DELETED an exception (made the odd path match the others) rather than adding a system. Gate-0's "refute the frame" is what caught it — a confident wrong frame at EVALUATE would have sailed through to a 10x-larger wrong build.
**Lesson — a multi-stage handoff must be TRACED end-to-end against the consumer's guard conditions, not assumed.** The crux: still-working keeps isStreaming=TRUE, but the handoff target (_postDisconnectUncertain block :1236) requires !isStreaming. Gate-1 flagged this as the load-bearing risk; Gate-2 then PROVED the 2-stage completion: store 90s watchdog → desyncConvergeVerdict clears isStreaming when backend terminal-idle → NEXT tick's :1236 block pulls. Two different blocks, two ticks, gated by store phase + backend state. "I set the flag" ≠ "the flag gets consumed" — trace which block actually fires, in what order, under what guard.
**Lesson — reusing a guard means re-deriving its cap's anchor in the NEW context (the BLOCKER).** _reconcileStreamStart feeds forceClearStreamVerdict/desyncConverge's 120-min cap as `now - _reconcileStreamStart`. The send path stamps it; the disconnect path did not — so activeGuardAge was ~Date.now() (cap pre-blown) → premature force-clear in the backend dead→cold gap. Reusing the verdict functions REQUIRED stamping the anchor they read. (Same family as O030: re-derive a reused guard's justification in its new context.)
**Disclosed follow-up (Gate-2 MEDIUM-1, OUT OF SCOPE):** the heal-grace sibling handoff (:3609) OMITS the _reconcileStreamStart stamp this fix adds — it may be the buggy sibling (its activeGuardAge huge → caps pre-blown). Needs separate verification of its store-phase dependency before touching. Filed, not silently fixed (blast-radius discipline).

### 2026-06-29: Credential-expiry fail-fast + visibility — a "False" that conflates expired with network is the bug (run_798d14eb, bugfix)
<!-- ref:0 | last:none | decay:active | source:pipeline -->
**What:** Expired isengard/ADA tokens stalled the main inference path (spinner forever) + were invisible. Root cause (observed live, not inferred): `CredentialValidator` existed but `is_valid()` had ZERO live callers — no spawn pre-flight; `/health` had no auth field so the 30s poll never learned creds expired; 401s only hit the log. The `exit -9` SIGKILLs in the log were a RED HERRING (every one preceded ~20ms by our own `force_kill_tree` idle-reclaim; memory pressure=ok, no OOM). The real signal was 100× `ada … isengard 401` and the dead validator. Fix: tri-state `check()` drives a spawn pre-flight (expired→fail-fast SSE, unknown/valid→fail-open) + a `/health` auth field + a poll-driven `CredentialBanner`.
**Lesson — collapsing every failure into one boolean DESTROYS the distinction the caller needs (Gate-1 BLOCKER1).** `is_valid()` returned `False` for BOTH expired AND a transient STS network blip (it caught everything → RuntimeError → False). A pre-flight built on it would falsely BLOCK a valid user on any network hiccup — strictly worse than the stall it fixes. The fix was a tri-state `check()` whose `_call_sts_typed` PRESERVES the botocore exception type: `expired` only on definitive auth signals, `unknown` (fail-open) on network/throttle/5xx. When a check gates a user-facing action, "did it fail" is not enough — you need "did it fail in a way that means STOP, or a way that means PROCEED-cautiously."
**Lesson — an STS error code is not a 1:1 map to "expired" — `AccessDenied` is the trap (Gate-2 H1).** `sts:GetCallerIdentity` can return `AccessDenied` via an SCP / IAM boundary / VPC-endpoint policy while the principal STILL has valid Bedrock access. Mapping it to `expired` (as the first cut did) would permanently false-block a working user in those environments. Only `ExpiredToken*/InvalidClientTokenId/UnrecognizedClientException/InvalidToken/InvalidAccessKeyId/SignatureDoesNotMatch` are definitive; `AccessDenied`→`unknown`→fail-open. The adversarial gate caught this; the happy-path tests never would have.
**Lesson — a cache TTL must be reasoned against BOTH failure directions AND the consumer's poll rate.** A 30-min success cache masked mid-session expiry (BLOCKER2 — stale `True` → spawn proceeds → stalls); a 60s TTL under a 30s poll did one STS call every OTHER poll (L1). Fix: cache ONLY `valid`, for 90s (≥ poll interval) → ≤1 STS/poll, and never cache `expired`/`unknown` so post-`mwinit` recovery is immediate. The TTL is not "how long is fine to be stale" — it's a 3-way tradeoff (stale-positive harm × stale-negative harm × probe cost).
**Known limitation (disclosed, not hidden):** the pre-flight only covers COLD spawn. A WARM session whose token expires mid-life can still stall on the streaming-retry path — but the poll-driven `/health` banner still flips to `expired` within ≤90s, so the signal is visible (strictly better than the silent stall). Full retry-path pre-flight deferred: it would expand blast radius into `retry_manager` for a case the banner already surfaces (R25).

### 2026-06-28: COMPLETE was the only pipeline step without a fresh-read stage doc — F004 root-fix, not a prose patch (run_b2b58e61)
<!-- ref:0 | last:none | decay:active | source:pipeline -->
**What:** The pipeline's COMPLETE summary box (`━━━ ✅ Pipeline COMPLETE ━━━`) intermittently "disappeared" from runs. Diagnosis (observation, not inference): the format spec was NOT deleted — it lived inline in INSTRUCTIONS.md at lines 757-862, ~750 lines past the file-head execution entry. Every OTHER stage (evaluate…reflect) has a `stages/<stage>.md` the orchestrator reads FRESH at execution time; COMPLETE (Step 6) was the ONLY step relying on the one-time activation read. By the time a run reaches COMPLETE (hundreds of tool-calls later), line 757 has decayed out of the attention window — textbook F004. Fix: extracted the spec to a new `stages/complete.md`, rewired Step 6 to fresh-read it, added the §3c dispatch-table row. INSTRUCTIONS.md 1614→1520.
**Lesson — F004 is structural, not behavioral: put enforcement text in the reading path AT the decision point, never "somewhere in the doc."** The other 8 stages weren't disciplined about emitting their landmarks — their docs are just read fresh when needed. COMPLETE failed precisely because it was the architectural exception. The fix wasn't "remind harder," it was "make COMPLETE identical to the others." When one item in a uniform set misbehaves, check whether it's the one that broke the pattern (R25: eliminate the exception, don't patch around it).
**Lesson — extraction (R4) needs a byte-equivalence check against git HEAD, and "diff shows whitespace-only" is the proof.** The AC4 verify diffed the new file's format lines vs `git show HEAD` 757-862; the only delta was 3-space list-indentation (the old block was nested under a numbered list item). Gate-2 independently re-ran the diff and confirmed. For any "I just moved it" claim, the cheap proof is a normalized diff against HEAD — not "I'm sure I copied it right."
**Lesson — adding a row to a prose dispatch table is safe ONLY if the consumer is the agent, not a parser.** The real break-risk was: does listing "complete" in the §3c table make `pipeline_validator.py` treat it as a profile stage needing an artifact? Refuted by reading code — the validator uses `get_profile_stages` from `pipeline_profiles.py` (where "complete" appears in NO profile), never parses the markdown. Verify the consumer of a table before assuming a new row is inert.

### 2026-06-27: READ-path "alive ≠ correct" is THE differentiator — and an eval contract, not a prose rule (run_6d5f60dd follow-up)
<!-- ref:0 | last:none | decay:active | source:manual -->
**What:** XG named the core line: the READ path — recall search + context-assembly + resume-extraction — is what separates this agent from any other. "守住这个 bar" or be a chatbot that *pretends* to remember. The trigger was `build_resume_context` silently filling ~4% of a 150K budget (hard-coded item caps), no error, tests green. A scan of all 172 golden cases for READ-path coverage **falsified my own "batch of theater to clean up" premise**: 29 cases touch the READ path, and only ONE (`GS_LOP005`) was theater — `python -c 'import build_resume_context; print("OK")'`. The other 28 (`file_contains` greps + `GS_RCHAIN_*` canaries) are substantive; their missing-negative is legitimate (a grep's negative is artificial, GUI33). So B's scope collapsed from "audit 172" to "the one import-OK case guarding the subsystem I just rewrote has zero behavior assertion."
**Lesson — "alive ≠ correct" on the READ path:** an eval that asserts a recall/resume/context function "imports / loads / returns non-empty / is alive" tests NOTHING about whether it did its job. This is the eval-layer twin of the test-theater class (GUI16/PIT13): the failure (4% fill) lives in the BEHAVIOR, and an existence-check is structurally blind to it. Any READ-path case must assert SUBSTANCE — fill ratio, did-both-legs-run, content-correctness — with a negative that goes RED when the behavior regresses (e.g. caps pinned back to fixed → fill collapses → red).
**Lesson — cognition is the anchor, the golden case is the guard.** Prose rules have not held me (CLASS A 12×); the durable fix for "don't let this regress" is a mutation-proven eval case, not a SOUL line. So this lesson is paired with a `resume_fill` probe + upgraded `GS_LOP005` — the rule and its enforcement ship together. This pairing ("锚点 + 守门") is the template for protecting any load-bearing capability going forward.
**Lesson — the wide-scan WAS the Gate-0.** Choosing B (wide) then measuring is what shrank the scope correctly; had I just built B's "batch upgrade," I'd have solved a non-existent problem (C039). Measure the gap before sizing the fix — the scan that proves there's nothing to do is not wasted, it's the cheapest correct outcome (PIT15).

<!-- ref:0 | last:none | decay:active | source:pipeline -->
**What:** User reframed the core: *"we want accurate + capable, not the few seconds latency — that is the core."* I had been optimizing recall for first-token SPEED (keyword-only sync + 400ms timeout that silently dropped recall; vector deferred to a background task). Reversed it: recall now runs BOTH legs (keyword + vector) to COMPLETION synchronously before generating. Deleted the 400ms daily timeout + ALL the M2 async machinery (net simpler). Replaced with an 8s DISASTER timeout that only bounds a code-hang + loud-on-ANY-degradation.
**Lesson — a "disaster timeout" is only real if EVERY blocking call inside the uncancellable thread is itself bounded < the cap.** `asyncio.wait_for(asyncio.to_thread(...))` cannot cancel the thread — it just stops awaiting. I bounded sqlite (busy_timeout 5s < 8s cap) and called it done. Gate-2 found the embed path: `EmbeddingClient` is 3×3s retries = 9.6s, AND `RecallEngine` embedded the query ONCE PER STORE (3×) → worst case ~29s in the orphaned thread. **A timeout that wraps an uncancellable operation is theater unless the operation's own internal timeouts sum to less than the cap.** Fix: embed once (3→1), and the per-call timeout × retries must fit the budget.
**Lesson — "I fixed the silent dead-path" is the most dangerous thing I can believe.** I made `_recall_for_query` loud (W5). Gate-2 found the SAME silent-empty bug one frame deeper: `RecallEngine.search()` swallows per-leg failures to `[]`, and `EmbeddingClient.embed_text` returns None (never raises) — so a fully-degraded recall returns empty-but-no-exception → the caller's loud guard never fires. **A degradation signal must be surfaced at the layer where the degradation HAPPENS, not assumed to propagate as an exception.** Fix: `search()` now records `last_search_errors`; the caller logs+counts when legs errored. The dead-path class doesn't get fixed by making one layer loud — it relocates one layer down until every empty-return path distinguishes "no match" from "errored."
**Lesson — a reversal ACTIVATES latent inefficiencies.** The 3×-per-store embed was harmless when embed was off-path/background. Moving recall onto the synchronous critical path turned a tolerable inefficiency into a cap-breaking hazard. When you move code onto a hotter path, re-audit what was "fine" on the cold path.
**Gate value:** Gate-1 BLOCKED twice (cap theater for sqlite + verify-untrustworthy). Gate-2 FAILED the build on 2 HIGH that I'd have shipped (embed-path theater + silent-hole-one-layer-down) — I only bounded sqlite and declared W5 fixed; both were false. Both fixed + re-verified in the convergence loop (embed 3→1 verified, leg-failures surfaced, mutation-RED on loud). Also: committed only my recall hunks of session_unit.py via a filtered `git apply --cached`, leaving a parallel session's in-flight soft-compaction work unstaged (STEERING #7).

### 2026-06-27: Closed the recall system — vector leg live + backfill convergence (run_e9b15722, goal)
<!-- ref:0 | last:none | decay:active | source:pipeline -->
**What:** Follow-up to run_bbd79e84. Two milestones via a goal pipeline. **M1 (backfill):** memory_vec was frozen at ~5% coverage for MONTHS — the recovery loop drained `LIMIT 10` orphans SERIALLY per session-close, so the daemon logged `recovered=10` every run and never caught up to ~425 entries. Fixed with a CONCURRENT batch drain (`embed_batch`, 5 at a time) capped at `_RECOVERY_DRAIN_BUDGET=200` → converges in ~3 cycles + `busy_timeout=30000` (WAL contention) + a `GET /api/recall/coverage` endpoint. **M2 (vector leg):** no prod path ran the semantic leg. Now a tracked background `asyncio.create_task` runs the vector leg OFF the first-token path and injects on the NEXT turn via `_inject_pending_vector_recall` — called at the TOP of `_maybe_inject_recall` BEFORE the `_recall_injected` guard. The keyword leg stays synchronous + keyword-only (run_bbd79e84 zero-embed invariant intact, verified). Task cancelled on all teardown paths.
**Lesson — the BLOCK that saved a re-ship of the SAME bug:** Gate-1 BLOCKED my first M2 plan because the next-turn injection sat AFTER `if unit._recall_injected: return` → **unreachable on turn 2** — the *identical* "wired but dead in prod" signature as the bug the whole project exists to fix. The guard against re-shipping it: a test that calls the path TWICE on the same unit (turn 1 sets the flag, turn 2 must still inject), mutation-verified (move injection after guard → RED). **When a fix adds cross-turn state behind a once-per-session guard, the test MUST exercise turn 2, not just turn 1** — a single-call test is blind to exactly the early-return that kills it.
**Lesson — WAL visibility is per-CONNECTION, not per-process (Gate-1 BLOCK-2 + Gate-2 correction):** I twice reasoned "read it inside the daemon so it shares the WAL view." FALSE — SQLite isolation is per-connection; a fresh reader (in-process or external) sees all COMMITTED rows equally. The real "external read returned 0" symptom was UNCOMMITTED singleton writes in the -wal that NO reader sees until commit. Don't attribute a visibility bug to process boundaries; attribute it to commit timing.
**Lesson — false comments are the "looks-wired" failure mode's actual home:** Gate-2 PASS found 3 MEDIUMs, ALL comment-drift (build_options-injection claim, 1-2-vs-3-cycle math, WAL rationale) — zero behavior bugs. Fixed all in-run. The code was correct, but the comments described wiring that didn't exist — exactly what the next debugging session trusts. A correct fix with a lying comment seeds the next dead-path investigation. P4: fix the comment now, not "later."
**Gate value:** Gate-1 BLOCKED twice (both real: unreachable injection + untrustworthy verify) → forced an in-daemon re-plan. Gate-2 independently ran 76 tests + a reachability trace against run_conversation, confirmed the run_bbd79e84 invariant held, and caught the comment drift. Both milestones mutation-verified (budget=10→RED, injection-after-guard→RED).

### 2026-06-27: Recall injection path was DEAD in prod — 150ms timeout vs 430ms embed (run_bbd79e84)
<!-- ref:0 | last:none | decay:active | source:pipeline -->
**What:** The post-first-message recall injection (`session_router._maybe_inject_recall`, awaited before the first response) injected ZERO knowledge in production for months. Root cause: a 150ms hard timeout vs real latency — FTS5 keyword recall is 145-265ms (races the wall, loses ~1/3) and a Bedrock embed is 430ms warm / 2343ms cold (never fits). Every first-message recall TimeoutError'd silently. Fix: (1) timeout 150→400ms (fits the keyword floor, measured 4/4); (2) `_recall_for_query` gained `allow_embed=False` default so the synchronous first-token path is keyword-ONLY (no Bedrock block — recall is "enhancement not critical path"); (3) new `MemoryRecallStore` adapter gives Memory the RecallEngine dict contract it never had (memory has no FTS table → keyword leg is a LIKE scan); (4) `FTS_SCORE_FLOOR` fixes a latent min-max bug that zeroed the worst keyword match (Gate-2 MEDIUM).
**Lesson — the "why every 'done' is half-done" answer, file:line:** the existing `test_recall_injection.py:103` **mocked `_recall_for_query`**, and the golden probes used an instant fake embed + bypassed the timeout wrapper. So 100% of tests validated *scoring correctness* and ZERO validated the *latency-vs-budget* reality that was the actual failure. This is the test-theater class (GUI16/PIT13): the test boundary drawn at the mocked function's input, the one integration fact that matters (real latency vs real timeout) skipped. **The missing test is always the one that exercises the real seam under the real constraint.** A recall test that mocks the recall function tests nothing about recall.
**Gate value (both fired):** Gate-0 (Understanding) caught my OWN premise was wrong — I told the pipeline "MEMORY ~15K<30K so selective injection doesn't fire"; live measurement showed 63K (>30K, it DOES fire) — the stale 15K came from KNOWLEDGE.md. Gate-1 BLOCKED the first plan: the Memory adapter "reuse MemoryEmbeddingStore as additional_store" would have silently returned zero (interface mismatch swallowed by RecallEngine's blanket try/except) = empty-but-looks-wired, the exact half-product class. Gate-2 independently mutation-tested (flip `allow_embed`→True → 3 RED → revert) AND found the FTS_SCORE_FLOOR latent bug my change made load-bearing (keyword-only leg → worst match zeroed → silently dropped). Dropped a planned pre-warm because it would have been dead code (GUI156). Follow-ups (documented, not deferred-as-evasion): vector-on-async-path + memory_vec backfill (5% populated) + the LOW full-table-scan in fts5_search.

### 2026-06-27: GS_RTH001 — __new__ test-harness attr drift (GC15 recurrence) (run_916d233f)
<!-- ref:0 | last:none | decay:active | source:pipeline -->
**What:** GS_RTH001 (runtime_health eval) was red: `fault_inject_recovery._build_bare_unit()` builds a `SessionUnit` via `__new__` (deliberately bypassing `__init__` for isolation) to drive the REAL retry loop, but didn't provision `_recycle_kill_pending` — which `retry_manager.py:215` reads on every retry iteration. Commit `6f968370` (06-26) added that read + an `__init__:652` default, but did NOT update the `__new__` harness → AttributeError → RECOVERY_FAILED. Fixed by provisioning the 3 retry-path parent attrs the harness was missing (`_recycle_kill_pending` + `_recovery_coordinator` + `_buffer_overflow_recovery`). Production was always safe (`__init__` sets the default); only the `__new__` harness drifted.
**Lesson:** This is **GC15 (new per-instance state field → grep ALL construction sites) extended to `__new__`-based test builders.** When a new `self.X` is added to a class `__init__` AND read in a hot loop, the grep for "all construction sites" MUST include `__new__`-based harnesses/test builders — they bypass `__init__` and silently drift. Mechanical guard for completeness: `grep -oE 'self\._parent\.[A-Za-z_]+' retry_manager.py | sort -u`, then diff against what the bare-unit builder provides (minus class attrs). Caught a related drift class: the prod path is safe precisely because `__init__` defaults it — which is exactly why the harness bug was invisible until the eval ran the real loop. GUI: a test harness that bypasses `__init__` owns the contract of self-provisioning every attr its target reads — and that contract rots every time the target gains a field.
**Gate value:** Gate-2 adversarial independently re-derived the 23-attr completeness, mutation-tested the fix (removing `_recycle_kill_pending` → reproduces the exact AttributeError = load-bearing), and caught 2 LOW comment-drift findings (a "24 reads" miscount + an undocumented OOM-branch latent-None risk) — both fixed in-run (P4, not deferred).

### 2026-06-27: negative_command teeth execution — opt-in via affirmative FAIL token (run_241be9da)
<!-- ref:0 | last:none | decay:active | source:pipeline -->
**What:** Wired `negative_command` teeth into `eval_canary_pass` — `negative_command` was dead config (gate_teeth + add_case required it to EXIST, but no eval path EXECUTED it; a vacuous canary whose marker survives a broken wire passed undetected). Now `verify_teeth` (default ON for CLI/nightly/GUI gate-report producers, OFF for the deadline-bound per-session health hook) runs the negative and requires it to discriminate.

**What worked:** TDD RED→GREEN; Gate-1 plan-attack folded 3 caller/false-pass gaps in before coding; Gate-2 adversarial (2 rounds) was the load-bearing gate.

**What FAILED (CLASS A authorship trap, recurred — caught only by Gate-2, NOT my tests):**
- My first design used **marker-ABSENCE** ("broken wire => OK marker gone => pass"). 7 self-authored tests all GREEN. But the contract was WRONG: a parallel session's `eval_spine_probe.py` uses the OPPOSITE convention — its negative prints `<NAME>_OK` + exit 0 on a *successful* break (marker PRESENT). The two conventions are **output-identical for "success"** → no single rule discriminates → marker-absence false-FAILED 2 live spine cases. Self-authored green tests inherit the author's blind spot; only the mutation-running adversary caught it (would have flipped the nightly gate RED).
- Marker-absence is **unfalsifiable**: a typo'd (exit 127) / no-op (`true`) negative prints nothing → passed vacuously (HIGH-1).
- **Stamp-staleness silent gate-defeat:** adding `negative_expected_contains` to the 8 stamped cases without re-running `stamp_golden_cases.py` made `compute_bvt` drop all 8 → teeth protected ZERO gate cases until re-stamped (Gate-2 round-2 HIGH).

**Lessons (reusable):**
1. When a field has **multiple in-repo producers**, an implicit semantic WILL collide — make it EXPLICIT opt-in (here: `negative_expected_contains`, an affirmative FAIL token the negative must emit).
2. A teeth/discriminator check must require the negative to **affirmatively signal it broke the wire**, not merely fail to print success. "Absence of X" is unfalsifiable.
3. Any edit to a **stamped** golden-case body MUST re-run `stamp_golden_cases.py` — else the case silently leaves the BVT.
4. Match on **stdout only** for canary markers — stderr echoes command names/tracebacks that incidentally contain a marker.
5. Scope discipline (C039) held: a pre-existing `GS_RTH001` runtime_health failure (SessionUnit AttributeError, another session's domain) surfaced during the gate run — flagged in meta_review, NOT fixed.

### 2026-06-26: v1.21.0 release + DMG criterion + streaming-markdown throttle (run_087e097e)
<!-- ref:0 | last:none | decay:active | source:manual -->

**Three threads closed in one session: a 294-commit release, a stale skill criterion, and the streaming-markdown half-fix.**

**1. Streaming-markdown half-fix CLOSED (run_087e097e, commit dc9d3132) — resolves the GUI02/PIT02/GUI03 thread.** e96b45a9 had killed the O(n²) per-token markdown reparse by rendering PLAINTEXT during streaming — but that lost live formatting and caused an end-of-stream reflow jump. For a user with frequent long replies the plaintext+jump UX was WORSE than the jank it fixed. The complete fix keeps markdown visible DURING streaming but THROTTLES the value fed to `MarkdownRenderer` to ~100ms (`STREAM_RENDER_THROTTLE_MS`) via an extracted `StreamingMarkdownText` child — `ContentBlockRenderer` early-returns per block type and can't host a hook, so Rules of Hooks FORCED the extraction (confirms GUI03 prediction). Parse count is bounded by time (~10/sec), not token count: O(n²)→O(n·T·10), wins whenever token-rate > 10/sec (always true for SSE). Leading+trailing edge + unmount→remount guarantees the final token is never dropped.
  - **Meta-lesson (the real keeper):** a perf fix that ABANDONS a UX capability is a HALF-FIX. The right move is bounding the expensive operation's FREQUENCY (throttle the input), not removing the capability (drop markdown). Throttle the input, keep the rich output.
  - **What Worked — deployment verified by OBSERVATION, first try (the inverse of 2026-06-24's R16b failure):** confirmed both layers shipped via a timestamp chain — dist rebuilt 04:46 > my source edit 04:31 > .app binary 04:47:49, and daemon `.version` = HEAD `bbd069f0` healthy. Did NOT grep the minified bundle for the function name (that would mislead — names are mangled; absence ≠ not-deployed, the C038 trap). mtime progression is the reliable signal.

**2. COE01 class is BROADER than the visible CI failures — `maxfail` hides siblings (new lesson).** CI backend runs with `maxfail` and stopped after 3 failures in `test_eviction_grace_period.py`. Those 3 were a classic COE01 platform-divergence: tests pass on macOS dev (where `open_tabs.json` EXISTS → real `owned_session_ids()` returns live IDs → synthetic test units look like orphans → evictable), fail on Linux CI (file ABSENT → `owned_session_ids()` returns `None` → R6 §9.9 fail-safe REFUSES eviction → `assert evicted is True` fails). Fix = autouse fixture pinning `owned_session_ids → set()`. **But the adversarial review on this "trivial CI-unblock" caught 2 MORE latent files** (`test_session_router_wake_grace.py`, `test_eviction_queue_before_force.py`) with the identical unmocked-`owned_session_ids` defect that `maxfail` had hidden downstream. Verified by forcing the `open_tabs` path absent (CI simulation) → all 3 green. **Lesson: when CI uses `maxfail`, the visible failures are a LOWER BOUND on the failure class. grep+run ALL sibling files exercising the same code path before declaring the class closed. Adversarial review earns its keep even on a "trivial" test-only change.**

**3. Release over-scope + push-before-tag saved the release.** v1.21.0 shipped 294 commits since v1.20.1 (tag was 8 days stale; **229 of those commits had never run CI** — origin/main was 229 behind HEAD). `s_swarm-release` skill text says >40 commits = BLOCK, but R11 allows explicit user sign-off — treated XG's 2 explicit confirmations as a logged over-scope override. **Critical save: pushing to origin BEFORE tagging triggered CI on the 229 unverified commits → caught the 5 stale-test failures (COE01 + tab-bar) that would otherwise have shipped inside a released DMG.** Lesson: push → CI-green → THEN tag/release; the unpushed-commit count is a hidden CI blind-spot that grows with tag staleness. Also fixed: `s_swarm-release` Stage 6 DMG criterion `>30MB` was empirically false (6 releases all ~10.3MB — the PyInstaller backend ships separately via daemon auto-install; the DMG is the Rust Tauri shell only) → corrected to a `>5MB` broken-build floor with a "do not raise toward 30MB" guard comment.

### 2026-06-24: Truncated-render reconcile-gap — re-queued thunk never drains (run_3c17790d, cf3b1e7c)

**The bug (P0, user-facing):** Assistant text reply renders TRUNCATED — cuts off mid-message, a "Continue" button appears as if the turn halted. Backend is HEALTHY: full reply generated (result_usage emitted), clean streaming→idle, content persisted to DB. The frontend DISPLAY layer drops the tail. Tab-switch restores it → render-layer timing bug, not data loss.

**Root cause (pre-existing since MessageStore birth 8b1ae191 — NOT the recent AskUserQuestion commit it temporally correlated with):** `MessageStore._fetchAndReconcile`'s `finally` only did `_reconcileInFlight--` and never drained a re-queued reconcile thunk. Sequence: (1) `endStreaming()` flushes the queued thunk → starts async `_fetchAndReconcile`, inFlight=1; (2) ~200ms later `scheduleTurnEndReconcile` calls `reconcile(freshFullDb)`; (3) `reconcile()` hits `_reconcileInFlight>0` (MessageStore:209) → NO-OPs + re-queues; (4) the in-flight fetch resolves with the STALE truncated snapshot, does inFlight--, but NEVER drains the re-queued thunk — and a finished turn has no further `endStreaming()` to flush it → truncated buffer stays permanently. **Fix:** drain the pending thunk in the `finally` when inFlight hits 0 at idle (terminates after one drain — re-queue only happens at phase==='streaming'; respects the `_reconcileGen` ticket; guarded on `!_destroyed`).

**What Worked:**
- **Empirical discriminator before coding (GUI01):** "does tab-switch restore the text?" cleanly split render-layer (YES) vs data-layer (NO) BEFORE touching code. Traced the code to predict YES → render-layer → narrowed the hunt to reconcile timing, not the partition logic (PIT51 stayed untouched).
- **git falsified the collision fear (STEERING #7):** a sibling session was actively committing a parallel "backstop-clock" fix on the SAME bug class. Instead of checkpointing/handing off, `git show --stat` proved that line touched MessageStore.ts **0 times** across its 4 commits — orthogonal (different file, acts on phase=streaming vs my phase=idle). Verify the overlap, don't assume it.
- **Adversarial 6-vector attack (infinite-loop / sync re-entry / gen-ticket / backstop / destroy / guard===0) all CONFIRMED SAFE** — the termination proof (re-queue only at streaming) is the load-bearing argument; the sub-agent independently verified it.

**What Failed — MY cognition, repeatedly (R16b, the real lesson of this session):** I asserted DEPLOYMENT/runtime state from inference instead of OBSERVATION **four times**, each wrong, each corrected by the user or by evidence: (1) "frontend not deployed" (read build-skill doc saying backend-only) → binary-embed check proved it WAS; (2) "strings can't find it = not embedded" (forgot Tauri compresses embedded assets); (3) "collides with the backstop-clock line" (timing coincidence) → git proved orthogonal; (4) **even after the user explicitly said "I just built and deployed"**, I kept insisting the fix wasn't deployed — until I checked binary mtime (00:42) > dist mtime (00:41, contains the fix) and was forced to admit it shipped. The pattern is unambiguous: for "is X deployed / running" I default to inference, and confidence does not correlate with correctness. **Observation (mtime, embedded content, live endpoint) is the ONLY valid proof — never the skill doc, never the tool-output shape, never the commit log alone.**

**META-SIGNAL (bigger than this bug):** This is the **~33rd commit** (full history) fixing the streaming/reconcile/truncated-render/stuck class — 20+ in the last 30 days alone (reconcile-gap PART 1/PART 2/Root-1 P3/H2/Gate-2/backstop-clock/this drain fix…). The frontend reconcile mechanism is SwarmAI's #1 recurring bug source. Each fix is a patch on the same "reconcile races and drops content" race model; patches stack on patches. The structural question — is the turn-end-reconcile-vs-in-flight-fetch race model itself the wrong design (vs e.g. a single serialized reconcile queue with guaranteed drain) — is unaddressed and worth a dedicated design session, not another patch.

### 2026-06-24: Tool-call XML leak guard — sample-encoded discriminator (run_e607c4cd)

**The bug:** Under long/meta-heavy sessions the model occasionally emits tool-call SYNTAX (`<invoke name=...><parameter ...>`) as RAW text instead of a real tool_use block. SDK parses it as a TextBlock → raw XML persisted to messages DB (8 confirmed cases 2026-06-24), rendered as half-XML, turn ends with no tool execution ("response stopped mid-way"). Backend turn completes NORMALLY (result_usage emitted) — so the empty-result/zombie guards never fire. This is a content-level malformed-output class, NOT a subprocess/API failure, and needs its own detector at the DB-persist gate (the AssistantMessage TextBlock branch).

**What Worked:**
- **Adversarial review caught a CRITICAL false-positive I would have shipped (RP42).** My v1 detector keyed on the `call\n<invoke name=` prefix — it matched all 7 observed DB leaks, so DB-validation gave me false confidence. The fresh-context adversarial sub-agent's generalization attack showed that shape is EXACTLY what an assistant turn emits when DOCUMENTING the bug (this very pipeline's summary, the source comment, the test file) → it would wrongfully kill any meta turn discussing the syntax. DB-validation proves recall on seen cases, not precision on unseen legit ones.
- **Three orthogonal signals beat one tuned regex:** (1) strip ``` fenced + `inline` code (docs are fenced; a real leak is raw), (2) match the structural BODY (`<invoke name="X">` + `<parameter`/`</invoke>`) not the optional `call` prefix — also catches no-prefix/CRLF variants, (3) require NO real ToolUseBlock in the message (if a real tool_use exists, sibling `<invoke>` text is discussion). Layered structural discrimination >> one clever pattern.
- **Real SDK types in the integration test.** First draft used a duck-typed fake AssistantMessage — `isinstance(block, TextBlock)` wouldn't match, silently routing to the zombie path so the test passed for the WRONG reason. Switched to real `AssistantMessage`/`TextBlock`/`ToolUseBlock`; leak test asserts the SPECIFIC guard error fired, not zombie.

**What Failed (caught in convergence, not shipped):**
- v1 `call`-prefix discriminator (CRITICAL false-positive) + missed no-prefix/CRLF/late-block leaks (HIGH false-negatives) + a 4000-char scan cap that truncated late leaks. All fixed in convergence iteration 1.
- kill()-before-raise was not exception-safe — if kill() threw, the retriable RuntimeError was skipped and the error degraded to a non-retriable crash, bypassing the --resume path. Wrapped kill in try/except so the raise is always reached.

**Lesson → RP42** (REVIEW_PATTERNS.md): a discriminator derived from observed positives can encode the SAMPLE not the CLASS. For high-false-positive-cost guards, build an explicit NEGATIVE corpus of look-alike legit outputs and prefer orthogonal structural signals.

### 2026-06-23: Session Isolation Audit + interrupt() pending-clear gap (run_2238b50b)

**What Worked:**
- **3-parallel-sub-agent audit (isolation / lifecycle / resume) with COE history fed in** — each agent verified prior incident fixes against live code + cited file:line, then I verified every flagged GAP myself. Isolation verdict: SOLID (agent actively hunted the cross-tab leak path, couldn't find one — every `setMessages` is behind a strict active-tab gate). High signal-to-noise: 3 maps of a hot zone in parallel, ~10min wall.
- **Verify-the-sub-agent rule paid off** — the lifecycle agent reported a "NameError: exc unbound" GAP1 that did NOT exist (it hallucinated `logger.debug(..., exc)`; real code is `exc_info=True`). Reading the actual lines killed a fake finding before it became a fix. Never trust a sub-agent's claim about your own code without reading the cited line.
- **Placement reasoning before coding** — the interrupt() fix had a real trap: clearing `_pending_tool_use_id` at the top would wipe state a concurrent new-send turn owns (stale-interrupt path). Tracing the 3 exit paths FIRST located the only safe spot (success→IDLE branch, after the stale guard returns). The companion `test_interrupt_stale_preserves_pending` locks that invariant.

**What Failed (the gap itself):**
- **Symmetric-cleanup miss** — `interrupt()` cleared `_active_agent_tools` but not `_pending_tool_use_id`/`_pending_question`, while `_cleanup_internal` + 3 other teardown sites cleared all of them. A guard set at emit-time (AskUserQuestion) must be cleared on EVERY teardown path, not just the kill paths. This is GC15 (`_content_emitted` reset-list) generalized: when you add a per-turn guard, grep ALL the places its siblings get cleared and match them.

**Pattern:** Stop-during-WAITING_INPUT is a teardown path that keeps the subprocess ALIVE (→IDLE), so it bypasses `_cleanup_internal` (which only runs on kill). Any "release the guard" logic must therefore be duplicated into the alive-stays-alive interrupt success branch, not assumed to ride on cleanup.

### 2026-06-07: Streaming P0 Cluster — 5 Bugs, 1 Root Cause Pattern

**What Worked:**
- **Structural reconciliation > string matching** — `_confirmed` marker + replace-not-dedup eliminated text duplication by construction. No amount of `endsWith`/`startsWith` heuristics can cover all edge cases (whitespace, prefix, re-emission). Making the wrong thing structurally impossible is the only robust fix.
- **Adversarial review caught 2 HIGH bugs** the author missed — `appendTextDelta` corrupting confirmed blocks, and same-turn re-emission dedup needing tool-boundary guard. Fresh context beats self-review every time.
- **Backend liveness events (thinking_progress)** — simple 15-line change to `sse_with_heartbeat` that eliminates both the stall warning AND gives users elapsed time feedback.

**What Failed:**
- **Streaming is highest-risk, lowest-coverage surface** — 700+ backend tests, 0 integration tests that simulate real SDK event sequences (20 text_delta + 5 tool_use + 3 assistant events in realistic order and timing). All streaming tests are unit-level against `updateMessages` in isolation.
- **Dev environment ≠ production regime** — Dev uses short conversations (<100K context). Production uses 1.4M context + 40+ tools/turn + 12min extended thinking. Timing, batching, content redaction all behave differently at scale. Every streaming P0 was invisible in dev testing.
- **"Clever" over "Structural" design** — `endsWith` dedup was elegant (5 lines) but assumed byte-identical text. One trailing `\n` difference → P0. Pattern: any logic that relies on "detecting" or "matching" instead of "preventing" is a time bomb.
- **Stall detection had no concept of thinking phase** — 60s threshold designed for text generation, applied universally. Extended thinking (redacted content, 12min silent) is a fundamentally different regime that needs its own UX treatment.
- **No "large context" QA gate** — Changes to streaming code were validated only with short conversations. Need a mandatory smoke test on 1M+ context session after any streaming change.

**Known Limitation (deliberate tradeoff):**
- **Cross-turn identical text dedup false positive** — If two separate turns produce identical text (≥20 chars), same-turn dedup (`startsWith` check) collapses them into 1 block. This is extremely rare in practice (agent almost never produces byte-identical ≥20 char text across turns). Tradeoff accepted: P0 fix (content explosion → spinner hang) > edge case (identical cross-turn text merged). **Structural fix direction:** backend should tag each assistant event with a monotonic turn index; frontend replaces "block from same turn index" instead of matching by string content. This eliminates ALL heuristic matching — true "impossible by construction."

**Lessons (systemic, not per-bug):**
1. **Streaming needs integration test harness** — Replay real SDK event sequences from production session logs. Not mock, not unit — real timing, real event order, real content shapes.
2. **"Make wrong impossible" > "Detect wrong after"** — `_confirmed` marker eliminated an entire class of bugs. Any future "match and filter" logic in the hot path should be replaced with structural markers. NOTE: same-turn dedup still uses `startsWith` heuristic — this is a known deviation from the principle, accepted as P0 tradeoff until turn-index tagging is implemented.
3. **5 P0s in 1 cluster = architecture gap, not 5 bugs** — Every fix touched `useChatStreamingLifecycle.ts`. The file is 2600+ lines with 19 exported functions and no isolation between streaming phases. Extraction into focused modules (reconciliation, stall detection, tab routing) would make each testable in isolation.
4. **Production testing for production regimes** — After any streaming change, verify on a real 1M+ context session with extended thinking. Can't simulate 12-min thinking silence in unit tests.

### 2026-05-16: Strangler Fig + Pipeline Self-Improvement Session

**What Worked:**
- **Thin delegates for zero-test migration** — 382 lines moved from hook → orchestrator, 0 test files changed. Delegate stubs (3 lines each) maintain backward compat. Commit f7824b12.
- **RP compound effect** — RP33 (multi-shape) caught today's CI bug class. RP34 (shell scope) caught F3 from PE review. Each RP prevents an entire class, not one instance.
- **Adversarial tier override (mechanical)** — diff > 100 lines = full tier regardless of profile. Would have caught today's F3 if it existed before the run.
- **Auto-discover > opt-in for gates** — Pollinate validator changed from `data.get("content_dir")` (opt-in, never fires) to auto-discover from `Knowledge/Pollinate/` (opt-out, always fires). Default path must trigger the gate.
- **Specialist ↔ REVIEW_PATTERNS sync** — Each specialist now references domain-specific RPs. Prevents the "two systems, different coverage" drift (LL12 recurrence).

**What Failed:**
- **First pollinate validator commit had 3 bugs** — sys.path pollution (F1), opt-in gate never fires (F2), indentation error (if outside try). All caught by same-session PE review. Root cause: "quick fix" mindset — wrote it fast, didn't trace the execution path mentally before committing.

**Anti-Patterns Encountered:**
- **Opt-in mechanical gate** — If the gate requires the actor to opt-in (add a field, pass a flag), it's not a gate — it's a suggestion. Real gates fire on the default path.
- **Specialist prompt drift** — Adding RP patterns to REVIEW_PATTERNS.md without syncing to specialist prompts = new knowledge that adversarial review can't use. Same root cause as LL12.

### 2026-05-20: Slack Human Experience — Pipeline Gap Analysis

**What Worked:**
- **TDD caught CJK complexity bug immediately** — first test run exposed `len(text) < 50` doesn't work for CJK (19 chars = 38 semantic weight). Fixed in 2 minutes.
- **Adversarial sub-agent found double-send (HIGH)** — human_mode path fell through to generic fallback, posting response twice. Tests couldn't catch this because mock adapter accepts any call.
- **Word boundary fix from adversarial** — "stop" matched "stopwatch" in redirect detection. Non-obvious from unit tests (test inputs never contain partial matches).

**What Failed:**
- **Protocol/Interface mismatch survived TDD + adversarial** — HeartbeatManager defined Protocol with `send_message_raw`/`update_message_raw`/`delete_message_raw`. SlackChannelAdapter has NONE of these. 24 tests pass (mocks). Adversarial said "Protocol looks fine." Would have crashed first real Slack message. Root cause: unit tests with mocks prove "if method exists, logic works" but not "method exists on the REAL object."
- **Parameter semantic mismatch survived everything** — `_post_ack(channel, text)` delegated to `send_typing_indicator()` which ignores `text` and hardcodes "Thinking...". Technically "works" (returns ts, no crash). But user sees wrong content. Root cause: function exists + signature compatible ≠ semantic contract satisfied.
- **Unnecessary latency survived everything** — `asyncio.sleep(2.0)` on every message for "merge window." Code-correct (no bug), but terrible UX for instant questions. Root cause: no user-path latency trace asked "what does user WAIT for?"

**Anti-Patterns Encountered:**
- **Protocol defined without verifying satisfier** — Protocol ≠ evidence that anyone implements it. Must grep the concrete class for each declared method.
- **Delegating to a method that "looks right" without reading its body** — `send_typing_indicator` sounds like it posts a message (it does), but it ignores the text parameter (reads its own hardcoded template). Method name is marketing, body is truth.
- **Adding sleep for "safety" on a user-facing path** — Every `asyncio.sleep(N)` on a request path is N seconds of user frustration. Justify each one against a specific data dependency, or remove it.

**Pipeline Improvements Made:**
- Step 3.6: Interface Seam Verification (build.md) — verify Protocol satisfiers exist + signatures match + semantics correct
- P6.5: User Path Latency Trace (deliver.md) — walk 2-3 user scenarios through real code, flag latency/silent-failure

### 2026-06-01: Thinking Block "不显示" — 7 层链路全绿,根因是 adaptive 统计行为

**What Worked:**
- **临时诊断日志定位空块假设** — 在 `session_unit.py` StreamEvent + ThinkingBlock 分支加 `TEMP_THINK_DIAG`,一轮对话即证明 Bedrock 上 Opus 4.8 发的是**完整明文** thinking(529 个非空 delta,12/12 块有内容 51~985 字符,0 空块),推翻了代码注释里"Bedrock returns empty thinking blocks"的旧假设。
- **7 层逐环验证 + DB/built-dist 实证** — 模型→Bedrock→`session_unit.py` 解析→`_persist_assistant_blocks` 持久化→`data.db`(45 条非空 thinking,最新 18:42)→`get_session_messages` 回传→`useChatStreamingLifecycle.ts`→`ContentBlockRenderer.tsx`。关键铁证:运行 app 内嵌的 `desktop/dist/assets/index-*.js` grep 直接命中 `appendThinkingDelta`/`>Thinking<`,证明不是旧构建、不缺代码。
- **统计分布解释感知** — 最近 500 条 assistant 消息仅 8%(43 条)带 thinking,65% 是纯工具调用。"看不到"是 `adaptive`+`effort=high` 下模型自行跳过思考的正常行为,非 bug。

**What Failed:**
- **代码注释/测试与运行实测不符** — `session_unit.py:2095` 注释 + `test_session_unit_preservation.py` 断言"Opus 4.8 over Bedrock 返回空 thinking 块"。当前版本(1.17.5,`claude-opus-4-8`)实测 Bedrock 发完整明文。注释成了误导,差点让人以为是上游 bug。Root cause:把某次特定条件的观察固化成了普适注释,没标版本/条件。

**Anti-Patterns Encountered:**
- **"compressed binary grep 0 命中"当成"代码缺失"** — Tauri brotli 压缩内嵌前端,直接 grep `.app` 二进制对所有前端字符串(连 `tool_use`/`ChatPage`)都 0 命中。差点误判。正解:grep 未压缩的 `desktop/dist/assets/*.js`(.app 的真实来源)。
- **查错 DB** — messages 表在 `~/.swarm-ai/data.db`,不在 `~/.swarm-ai/SwarmWS/data.db`。第一次查 SwarmWS 报 "no such table"。

**Known Issues / Decision:**
- **保持 `adaptive` + `effort=high`(官方最优解)**,不切 `enabled`。`enabled + budget_tokens` 在 Opus 4.6+ 已废弃(官方推荐 `adaptive + effort`,未来移除),且每轮强制思考会变慢变贵、挤占 1M context。8% 可见率是设计,不是缺陷。
- 完整诊断:`Knowledge/Notes/2026-06-01-thinking-block-7layer-diagnosis.md`
- TODO(可选):修正 `session_unit.py` 那段空块注释,标注"早期/特定条件"而非普适;或删除已不符的断言。
## What Worked / Failed — run_af36e709 (P4 streaming-never-finalizes, 2026-06-21)

- **[Worked] Two independent sessions diagnosed the identical root cause (H1+H2)** — cross-validation. Verifying a duplicate run before starting (run_242b5e07 vs run_af36e709) avoided double work; abandoned the redundant one, resumed the deeper one (had design doc + BUILD shell).
- **[Worked] Adversarial sub-agent caught a HIGH the author + plan both missed** — the inherited PLAN 's H2 cleanup only handled EMPTY placeholders. Continuation/drain paths (answer-question, queue-drain, permission, retry-timeout) keep NUMERIC ids and stream content IN, so at turn-end they are non-empty + uncorrelated → the unconditional turn-end reconcile duplicated them. Empty-only cleanup structurally could not catch it. This is the canonical "adversarial finds the 4th-7th interaction the author's mental model drops" (PIT39).
- **[Lesson] H2 (unconditional turn-end reconcile) is a load-bearing change with its own blast radius** — adding a DB-fetch-and-reconcile on EVERY result event surfaced rows that previously never entered the view for continuation turns. "Add a safety net" changed behavior for paths the net was not designed around. Always ask: which paths NOW reconcile that did not before?
- **[Lesson] A numeric message id is a reliable discriminator for "uncorrelated optimistic placeholder"** — verified all 8 numeric-id sites in ChatPage are assistant placeholders; no UUID or local-* id is purely numeric. `/^\d+$/.test(id)` is a safe structural signal, lower-risk than threading client_id through 4 call sites + backend.
- **[Failed/Process] Ran full-project `tsc --noEmit` unbounded → ~17min hang, user had to interrupt.** Violated my OWN just-earned rule (AGENT.md: "a hang IS a failure; long/background commands need timeout + give-up threshold"). macOS has no `timeout`/`gtimeout` — must use `perl -e 'alarm N; exec @ARGV'`. Per-file vitest runs were fine (2-4s each); the culprit was the unbounded whole-project type check. Lesson: scope type-checks to changed files OR trust vitest-covered tsc; never裸跑 a multi-minute command without a bounded wrapper + poll.
- **[Worked] Caught my own bug in adversarial self-check before ship** — tracer-1 wrote bare client_id on the assistant row, which would collide with the user placeholder in _applyMerge. Caught it in tracer-2 before commit landed in prod (the `-asst` suffix).

**Deploy note:** frontend change — requires `npm run build:all` + relaunch to take effect (NOT auto-deployed). 4 commits: a8e9b168, 60f2bdd7, c16a6271, 1b4081fe.


### run_c7747f5c — run-report qualitative-score crash (trivial, 2026-06-21)
- **[Worked] The named bug was already fixed; R7 same-class scan found the REAL one.** XG reported the roi `:.3f` crash, but HEAD (b5730fd9, parallel session) had already guarded it. The post-fix same-pattern scan caught the unguarded sibling at line 1906 (`{score:.2f}` on dimension scores from the same untyped `eval_scores` dict). Lesson: when a reported bug is already fixed, don't stop — grep the bug CLASS, the original report is often the tip.
- **[Worked] RED failed for the right reason** — `ValueError: Unknown format code 'f'` at the exact line, via the real date-scoped artifact loader (not over-mocked).
- **[Hazard] Auto-commit hook swept a parallel session's in-progress failing test into my commit.** `git add <file>` stages ALL changes in a shared file; the per-minute auto-commit had already staged the parallel session's `test_completion_blocked_when_validator_raises`. Chose NOT to amend (working tree showed the file modified again = parallel session still editing → amend would clobber live work, STEERING #7). Cost = cosmetic mis-attribution; alternative cost = destroying another session's work. In shared auto-commit repos, `git add -p` or commit only your exact hunks — but never rewrite history while a sibling is actively editing the same file.
- **[Decision/taste] Left the LOW unescaped-pipe finding unfixed** — cosmetic, pre-existing, identical on the roi line; fixing would expand scope into parallel-committed code. Flagged as follow-up.

### run_1a45cfe9 — SSE-disconnect zombie-poison root fix (full, 2026-06-21)
- **[Worked] Evidence-first diagnosis flipped my own causal hypothesis TWICE.** Started assuming "zombie causes disconnect" → logs proved reverse (disconnect→stop→poison→zombie). Then assumed "add seamless resume (buffer/--resume/auto-continue)" → reading code proved the fix is DELETION (a stale frontend /stop), not addition. P1 (verify don't infer) earned its cost: 2 sub-agents + 4 log digs overturned 2 confident-but-wrong theories.
- **[Worked] The fix was DELETING a stale 'Gap 2 fix', not adding code.** Frontend POSTed /stop on disconnect to avoid force_unstick→kill→replay. But backend's _recover_streaming_on_disconnect (2026-06-20) already transitions STREAMING→IDLE + soft-interrupt (leaves subprocess ALIVE). The frontend stop became redundant AND the actual poison source (its /stop→interrupt_session→kill corrupted the subprocess). STEERING #10 class: contract upgraded (backend soft-recovery), old consumer (frontend stop) not retired → it triggered the exact kill it meant to prevent.
- **[Worked] Adversarial Gate 2 caught a real coverage gap.** RED test only locked 1 of 2 deleted stopSession sites (_healGraceActive=true skips the heal-grace branch). Added a fake-timer test for the heal-grace-expiry site; verified RED-on-reintroduce. Two deletions need two tests (STEERING #11: every removed path needs execution coverage).
- **[Hazard] perl -i one-liner for temp regression-injection hit BOTH comment sites, leaving a stray `currentTab` ref in the terminal branch (undefined-var).** Full suite caught it (2 failures) on the post-restore run. Lesson: verifying-by-injection is fine, but a multi-match perl edit needs a full-suite GREEN confirm after restore — never trust "I reverted the one I added." The injection landed in 2 places; I reverted 1.
- **[Decision/scope] A fixes ONLY the poison (layer 2).** Layer 1 (WHY SSE disconnects — stall timer 90s / tab-close abort / reconnect race; 30 events/day) and layer 3 (seamless mid-stream resume) are deliberate follow-up runs. Honest scope: user may still see "send again" on a genuine disconnect, but no more zombie/stuck-Continue loop.

### What Worked — Decoupling a mislayered weekly job from a per-session hook (run_6ac3fc0b, 2026-07-02)

**Bug:** evolution_maintenance lifecycle hook ran a ~5-min evolution cycle (mine
3629 transcripts + Bedrock) synchronously on every session close. The 180s hook
budget (main.py:943) < the 293s cycle → asyncio.wait_for cancelled the coroutine
before the post-await state-write → `.evolution_last_run` never advanced →
every session re-triggered (59×/day), each spawning an uncancellable 293s zombie
thread. A flock made concurrent re-triggers cheap lock-rejections (so the cost
was 1 real cycle + N rejections per wave), but neither path advanced state.

**Fix (structural, 3 parts — the point):** the root was an ARCHITECTURAL
MISLAYER (a weekly 5-min job on a per-session hook budget), not the timeout
value. (1) Deleted `_maybe_run_evolution` + call site — the scheduled
`evolution-cycle` job is now the sole trigger; the hook keeps only its cheap
~7ms governance work. (2) Gave the scheduled job `timeout_seconds=1800` — it was
inheriting the 300s default. (3) Dormant-doc'd the orphaned
`.evolution_corrections_pending` writer.

**The load-bearing lesson — Gate-1 caught a "structural fix" that RELOCATED the
bug.** My initial plan (delete hook path → scheduled job sole trigger) was
correct in direction but the scheduled job inherited the 300s default = only 7s
over the 293s cycle. The first slow run would time out, never write state, and
after 3 Thursday timeouts the circuit breaker would DISABLE evolution entirely —
the same "silently stops advancing state" class, just moved from per-session to
weekly. A pre-code Gate-1 skeptic (reading executor.py:813/models.py:118) found
it before any code. **"Move the heavy work off the hot path" is only half a
structural fix — the new home must actually be able to FINISH the work.** When
decoupling A→B, verify B's own resource limits (timeout, budget, retry, circuit
breaker) can hold the workload; a decouple that hands work to an under-resourced
executor is 表面, not 根治.

**Also validated:** (a) the M3 Understanding-Gate skeptic caught 2 CLASS-B
factual errors in my own framing (asserted 30s timeout — really 180s; asserted
empty state file — really valid 2026-06-25; I'd read the class default + the
wrong filename). Observation beats memory, again. (b) Gate-2 confirmed a MEDIUM
(AWS creds stripped from script-job subprocess env) that was real but
non-blocking (~/.aws file-based creds survive env-strip) — flagged a deploy-time
smoke-run rather than a code change.
