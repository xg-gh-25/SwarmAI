# Manifest for persist (formerly save-memory)
name: persist
version: "1.0.0"
tier: always

scripts:
  - path: scripts/locked_write.py
    description: "Locked read-modify-write for MEMORY.md and EVOLUTION.md."

resources:
  - scripts
