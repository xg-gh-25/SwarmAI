---
name: mcp-builder
description: >
  Guide for building high-quality MCP (Model Context Protocol) servers that enable
  LLMs to interact with external services. Covers Python (FastMCP) and TypeScript (MCP SDK).
  TRIGGER: "build MCP", "create MCP server", "MCP tool", "new MCP", "MCP integration".
  DO NOT USE: for using existing MCPs (just call their tools), for configuring MCP connections
  (use settings), or for MCP Gateway architecture decisions (see MEMORY.md MCP Gateway hold).
tier: lazy
platform: all
---
# MCP Builder

> This skill loads full instructions on activation. Read INSTRUCTIONS.md before proceeding.

TRIGGER: "build MCP", "create MCP server", "MCP tool", "new MCP integration"
DO NOT USE: for using existing MCPs, configuring connections, or MCP Gateway decisions
