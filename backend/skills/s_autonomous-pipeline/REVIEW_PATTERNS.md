# Runtime Pattern Checklist (RP1-RP52)

This file contains the canonical list of recurring production bug patterns that code review and unit tests consistently miss. The REVIEW stage references this file as a BLOCKING check -- every applicable pattern must be explicitly verified or marked N/A.

## Patterns

| # | Pattern | Trigger (when to check) | What to verify | Example bug |
|---|---------|------------------------|----------------|-------------|
| RP1 | **subprocess timeout orphan** | `create_subprocess_exec` or `subprocess.Popen` | `asyncio.wait_for` timeout path has `proc.kill()` + `await proc.wait()` BEFORE re-raise | ffmpeg runs forever after 30s timeout (run_c2881d2f) |
| RP2 | **subprocess missing binary** | `create_subprocess_exec("some_binary", ...)` | `FileNotFoundError` caught -- user-friendly error message | ffmpeg not installed -- raw 500 instead of "install ffmpeg" (run_c2881d2f) |
| RP3 | **React hook cleanup** | new `use*` hook with refs or subscriptions | `useEffect` return releases all resources (streams, timers, listeners) | Mic never released on tab close (run_c2881d2f) |
| RP4 | **stale closure** | callback passed to hook/async uses component state | use `useRef` to hold latest value, read `.current` in callback | transcribed text overwrites user's typing (run_c2881d2f) |
| RP5 | **FormData Content-Type** | `new FormData()` sent via axios/fetch | NO explicit `Content-Type` header -- browser must add boundary | Multipart broken, backend gets 400 (run_c2881d2f) |
| RP6 | **setTimeout leak** | `setTimeout` in component/hook | timer ID stored in ref, cleared in cleanup effect | setState on unmounted component (run_c2881d2f) |
| RP7 | **error msg / constant mismatch** | error messages containing numeric values | message matches the actual threshold/constant in code | "need 0.5s" but constant is 1.0s (run_c2881d2f) |
| RP8 | **hardcoded env assumption** | region, URL, port, path as string literal | configurable via env var or parameter with sensible default | us-east-1 hardcoded, user in us-west-2 (run_c2881d2f) |
| RP9 | **API boundary naming** | new endpoint returns JSON consumed by frontend | backend snake_case fields have camelCase frontend interface + conversion function | `duration_ms` passed raw to TS instead of `durationMs` (Kiro review) |
| RP10 | **barrel export** | new hook, component, or service file created | exported from the directory's `index.ts` | `useVoiceRecorder` missing from `hooks/index.ts` (Kiro review) |
| RP11 | **SDK handler reassignment** | `self._handler = new_handler` or similar | old handler `.close()` called BEFORE reassignment; reset to `None` before restart after crash | Old SocketModeHandler leaked on reconnect (Kiro review) |
| RP12 | **unstable callback refs** | inline arrow functions passed as props to hooks | wrap in `useCallback` with correct deps; use ref for values that change every render | `onTranscript`/`onError` recreated every render (Kiro review) |
| RP13 | **state machine completeness** | type/enum declares states + design doc shows transitions | every declared state has >=1 code path that enters it AND exits it; every transition has a trigger | Voice `interrupted` state declared but no code ever sets it; VAD never calls `stopRecording()` -- `listening->processing` transition unreachable (2026-04-25 E2E review) |
| RP14 | **cross-service parameter mismatch** | function A calls service B with parameters mapped from A's inputs | parameter names/semantics match B's API contract; override values don't conflict with defaults | `voice_id="Zhiyu"` + `language="en-US"` -- Polly rejects because Zhiyu requires `zh-CN` (2026-04-25 E2E review) |
| RP15 | **setTimeout for state propagation** | `setTimeout(() => fn(), 0)` to sequence React state + side effects | verify the ref/state the deferred fn reads is set BEFORE the timeout, not by a concurrent React render | Voice send `setTimeout(0)` reads `inputValueRef` which could be overwritten by React re-render before timeout fires (2026-04-25 E2E review) |
| RP16 | **concurrent async without ordering** | N async calls fired in a loop with `.then()` enqueue | responses may resolve out of order; use sequential `await` or `Promise.all` + indexed insert | 3 concurrent Polly calls -- sentence 2 (short) resolves before sentence 1 (long) -- audio plays out of order (2026-04-25 E2E review) |
| RP17 | **unsanitized string in structured format** | user/dynamic text embedded in HTML, JSON template, SQL, or shell | escape for target format: `html.escape()`, `json.dumps()[1:-1]`, parameterized query, `shlex.quote()` | `<pre>{message}</pre>` with raw message -- XSS in email; `payload_template.replace("{title}", title)` -- JSON parse failure on quotes (2026-04-25 E2E review) |
| RP18 | **timezone-sensitive date boundary** | SQL uses `date('now')`, `datetime('now')`, or `strftime(...,'now')` for filtering (e.g. "today's records") | write and query use the same timezone; desktop apps should use local time (`datetime.now()`), not SQLite UTC `date('now')`. Tests always pass because test runner and DB share a timezone -- the bug only appears when user is in a different TZ from UTC. | `WHERE date(timestamp) = date('now')` but timestamps written in UTC -- "today" boundary is 8h off for UTC+8 user, tokens after midnight local show as yesterday (run_6823b0d4 E2E review) |
| RP19 | **deprecated stdlib API** | `asyncio.get_event_loop()`, `datetime.utcnow()`, `logging.warn()`, `imp.reload()` | use Python 3.12+ recommended replacements: `get_running_loop()` / `get_event_loop_policy().get_event_loop()`, `datetime.now(UTC)`, `logging.warning()`. Deprecated APIs emit warnings and may raise `RuntimeError` in future Python versions. | `asyncio.get_event_loop().create_task()` in async generator -- DeprecationWarning in 3.12, potential RuntimeError in 3.13+ (run_6823b0d4 E2E review) |
| RP20 | **nested clickable event propagation** | `<button>` or `onClick` handler inside another element with `onClick` or `<li onClick>` wrapping action buttons | inner handler calls `e.stopPropagation()` -- or event WILL bubble to outer handler, firing both actions on one click | Todo ✅ button inside `<li onClick={onItemClick}>` -- clicking "mark done" also populated ChatInput with the todo (Briefing Hub v2 review) |
| RP21 | **popover toggle + click-outside race** | popover with `mousedown` click-outside handler + a toggle button that opens/closes it | toggle uses `onMouseDown` + `e.stopPropagation()` (fires before click-outside), or click-outside handler excludes the toggle button via ref. Using `onClick` for toggle + `mousedown` for click-outside = race: close fires first, toggle reopens. | History search popover flickers on re-click -- click-outside closes it, then toggle button `onClick` reopens it in the same event cycle (Briefing Hub v2 review) |
| RP22 | **React ref/state desync on programmatic input** | code calls `setState(value)` for a value that is ALSO read via `someRef.current` in a synchronous path (e.g., send handler reads `inputValueRef.current`) | ref is synced alongside state: `ref.current = value; setState(value)`. If only state is set, any synchronous consumer within the same microtask reads the stale ref. React state updates are async; refs are immediate. | `handleItemClick` called `setInputValue(text)` but not `inputValueRef.current = text` -- fast ⌘Enter within the same render frame sent the previous message (Briefing Hub v2 PE review) |
| RP23 | **conditional layout with empty children** | CSS grid/flex container with N columns/rows where children are conditionally rendered (`{hasX && <Card/>}`) | when one side renders 0 children, the container still allocates space (empty column in `grid-cols-2`, empty flex child). Either: collapse to single-column when one side is empty, or use `display: contents` wrapper. | 2-col Welcome Screen -- only Signals on right, left column empty -- 50% of screen is blank (Briefing Hub v2 review) |
| RP24 | **cross-language serialization format assumption** | Language A produces JSON/text consumed by Language B (e.g., Python FastAPI → Rust parser, Node → Python, Go → TS) | verify the **exact byte-level format** each side produces/expects. Python `json.dumps()` uses `": "` (space after colon); compact JSON has `":"`. Rust/Go manual string matching, regex, or `split(":")` will fail on the spacing variant. Test with the **actual serializer output**, not a hand-crafted string. | Rust `get_daemon_version()` searched for `"version":"` (no space), FastAPI returned `"version": "1.8.4"` (with space) — version always `None` — daemon restarted every launch (run_91a6fb7e E2E review) |
| RP25 | **blast radius — system lifecycle not traced** | changeset touches infra, release, deploy, CI, or cross-service config | after diff review, list ALL system-level flows this changeset participates in (build→package→deploy→update→run). For EACH flow, trace the complete chain and verify every link is compatible. Specifically check: (1) existing code that CONSUMES what you built (SSM update script, CI workflow, provisioner) — even if you didn't modify it. (2) config propagation — does a new file/format reach all consumers? (3) cleanup/reload — if config format changed, does the runtime pick up the new config without restart? | Unified release pipeline (run_19129544): added Hive tar.gz packaging but didn't check SSM update script — it didn't reload Caddy after config changes. Added CI job but made it depend on unrelated macOS build (15 min wasted). Added rsync excludes but missed .DS_Store. All 5 bugs outside the diff, inside the system. (2026-04-29 DevOps audit) |
| RP26 | **deprecated API from current docs** | changeset uses framework-specific patterns (React hooks, FastAPI decorators, boto3 calls, Pydantic models, asyncio patterns) | for each non-trivial framework pattern, verify against **current official documentation** (not training data). Detect version from pyproject.toml/package.json. Check: (1) is this the currently recommended API? (2) are there deprecation warnings in the docs? (3) does the docs show a different/newer pattern? Source hierarchy: official docs > official blog > MDN/web.dev. Never trust Stack Overflow, tutorials, or training data as primary source. | `asyncio.get_event_loop()` deprecated in 3.12+ — passed pipeline because no check verified against current docs. `date('now')` UTC mismatch — SQLite docs clearly state this returns UTC, but training data habit used it for local time. Both from run_6823b0d4 E2E review. (2026-04-29 Agent Skills Adoption) |
| RP27 | **non-deterministic output ordering** | function returns list, dict, or set that callers display or compare | output ordering is either (1) explicitly sorted, (2) insertion-ordered (dict/list), or (3) documented as unordered. `set()` iteration, `dict.keys()` from hash-dependent sources (DB rows, JSON parse), or `os.listdir()` produce unstable order across runs. Callers that `assertEqual` or display to user will flake or confuse. | Audio sentences from 3 concurrent Polly `synthesize_speech()` calls — responses resolved in network-latency order, not sentence order — playback scrambled (C011, 2026-04-25). Inspired by Kiro PE review "Determinism" category. |
| RP28 | **schema migration without rollback path** | changeset adds/modifies DB table, column, index, or runs `ALTER TABLE` | migration has a documented rollback: either (1) `ALTER TABLE ... DROP COLUMN` reverse step, (2) migration is additive-only (new nullable column, new table), or (3) blue-green/shadow-write strategy for destructive changes. `NOT NULL` on existing column without backfill = data loss on rollback. Check: can we deploy the previous version after this migration runs? | No real bug yet — preventive pattern from Kiro PE review "Schema Evolution" category + industry best practice. SQLite `ALTER TABLE` is limited (no DROP COLUMN before 3.35.0) — verify SQLite version supports the operation. |
| RP29 | **YAGNI — unnecessary abstraction layer** | new interface/protocol/ABC with ≤1 concrete implementation, or new config/flag for a behavior that could be hardcoded | count concrete implementations. 0 = dead interface. 1 = premature abstraction (delete interface, use concrete type). New config key with only 1 possible value = unnecessary indirection. Ask: "if I delete this abstraction, does the code get simpler?" If yes, delete it. | Pytest safety hook v1: 130 lines with mutex, auto-rewrite, and 3 abstraction layers. After 4 rounds of user pushback, final version was 55 lines with zero abstractions (C009, 2026-04-12). Memory intelligence: 515-line 5-dimension weighted scoring, reduced to 60-line frequency gate after design review (KD07, 2026-04-22). |
| RP30 | **hook no-op path scaling** | code added to a per-session/per-request hook, cron, or recurring callback | analyze the **no-op path** (hook fires, finds nothing to do, exits): (1) what's the cost when it does NOTHING? (2) does that cost scale with data history or just recent data? (3) if O(total_history): add a time/size bound (mtime filter, index file, cursor/watermark). The no-op path is 99% of invocations — optimizing the action path while ignoring no-op cost is a structural blind spot. | Auto-cultivation hook scanned ALL 141 run.json files every session to find 0 uncultivated runs. Pipeline REVIEW + adversarial both missed it because they only analyzed the action path (correctness of cultivation). PE review caught the O(n) steady-state cost. Fix: 30-day mtime filter bounds scan to O(recent). (run_d73239fe, 2026-05-14) |
| RP31 | **synthetic-only test data** (test-theater twin: RP47) | feature processes data from an external source (DB, JSONL, API, user input) | at least one test uses **production-representative data** — not data crafted by the developer to match the implementation. Synthetic inputs validate the code path; real inputs validate real-world utility. RED flag: all test inputs were written by the person who wrote the code AND match the internal heuristic perfectly. | DDD T2 keyword classifier: 29 tests pass with synthetic corrections containing magic words ("daemon", "nc -z"). All 5 real corrections from production JSONL (narrative, behavioral) returned None — 100% false negative. Feature "worked" but delivered zero value. (run_76273219, 2026-05-15 PE review) |
| RP32 | **re-filtering curated input** | function receives input from a curated source (LLM extraction, human editor, upstream validator) AND applies its own relevance filter | if upstream already curated (existence = relevance), downstream should **route** (where does it go), not **re-filter** (should it go at all). A weaker heuristic downstream silently drops valid inputs that passed a stronger upstream gate. Anti-pattern: keyword regex gate on top of LLM extraction gate = double-filtering that removes signal. | `cultivate_from_corrections()` applied `_classify_lesson()` keyword gate to corrections already curated by DailyActivity extraction LLM. Corrections without technical keywords (100% of real corrections) silently rejected. Fix: corrections bypass keyword minimum, fallback to IMPROVEMENT.md "What Failed". (run_76273219, 2026-05-15 PE review) |

| RP33 | **multi-shape function return (branching code paths)** | function has `try/except`, `if feature_flag`, or fallback branch that produces a **different output dict shape** per path | ALL consumer tests must cover ALL return shapes. The function's docstring or type annotation must describe the union of shapes. If shape A has `{"docs": {"PRODUCT.md": {"exists": bool}}}` and shape B has `{"docs": {"PRODUCT.md": {"sections": {...}}}}`, tests using shape A will pass locally while CI (which has the import for shape B) hits KeyError. Specifically: (1) list every branch that produces output, (2) for each consumer test, verify which branch it exercises, (3) any untested branch = finding. | `_collect_ddd_health()` has `try: import compute_section_health` (5-dimensional output, no `exists` key per doc) / `except ImportError:` fallback (staleness-only, with `exists` key). Test created minimal fixture → CI imported the real module → dict shape mismatch → `KeyError: 'PRODUCT.md'`. 1735 tests pass, 1 fails — and it's the shape-mismatch branch. (v1.13.0 release CI, 2026-05-16) |
| RP34 | **shell variable scope across Bash tool calls** | a multi-stage script/skill sets a variable (e.g., `NEW_VERSION=...`) in one Bash call that a later Bash call references via `${VAR}` | each `Bash` tool call is an **independent shell** — variables from prior calls do NOT exist. Every Bash block that uses a variable must either: (1) re-derive it (`NEW_VERSION=$(cat VERSION)`), (2) read it from a file/env, or (3) receive it as a function argument. Specifically: grep for `${...}` or `$VAR` in the script block, trace where that variable is SET — if the set is in a DIFFERENT Bash call, it's a guaranteed bug. | `s_swarm-release` Stage 5 DMG check used `${NEW_VERSION}` which was set in Stage 2's Bash call. Stage 5 runs in a separate invocation → variable undefined → check always fails or silently expands to empty string → wrong DMG path. Fix: `NEW_VERSION=$(cat VERSION)` at top of Stage 5 block. (v1.13.0 release PE review, 2026-05-16) |
| RP35 | **shared executor/pool contention** | code adds `asyncio.to_thread()`, `run_in_executor(None, ...)`, or any blocking call (>1s) dispatched to a thread pool | enumerate ALL consumers of the same pool. If the new consumer can block >5s (subprocess wait, network call, file lock) AND the pool has latency-sensitive consumers (health endpoints, DB queries, SSE keepalives), the new consumer MUST use a **dedicated** `ThreadPoolExecutor`. Default asyncio pool is SHARED — one 480s block starves everything. Specifically: (1) list all `to_thread`/`run_in_executor(None)` calls in the module + imports, (2) find the longest-blocking one, (3) if max_block > 5s AND pool has <1s consumers → finding. | Job scheduler's `subprocess.run(timeout=480s)` ran on default asyncio pool. Health endpoint's `aiosqlite` also uses default pool. 4 concurrent jobs filled 8 default workers → health endpoint hung → Tauri showed "not responding". Fix: `_job_executor = ThreadPoolExecutor(4)` dedicated pool. (commit 8d9895c1, 2026-05-20) |
| RP36 | **fix-removes-failure-mode regression** | changeset fixes a bug that previously caused early failure (circuit breaker, bad path, missing config, wrong state) on a code path | ask: "Now that this path SUCCEEDS, what new load/behavior does it introduce? What shared resources does the newly-successful path consume?" A path that previously failed at line 3 now runs to line 300 — everything between line 3 and 300 just became reachable for the first time. Check: memory, thread pool slots, file handles, DB connections, network sockets consumed by the now-successful path. | b812e9e2 fixed job paths + reset circuit breakers → jobs successfully spawn 480s CLI subprocesses for first time → default thread pool exhausted → health endpoint hang. The fix was correct; the regression was what the fix ENABLED. Same class: removing a `return` guard, lowering a threshold, resetting a circuit breaker. (2026-05-20, LL01) |
| RP37 | **process tree lifetime mismatch** | code spawns a subprocess (build, test, long computation) from within a session/request that has a shorter lifetime | if the spawner can be evicted/killed (session timeout, tab close, app restart) while the child is still running, the child dies with it (SIGKILL propagates down process tree). Verify: (1) is the child's expected runtime > spawner's minimum lifetime? (2) if yes, child must either detach (`setsid`/`nohup`) or be spawned by a longer-lived process (daemon, systemd). The right fix is usually architectural: long-running ops belong on the daemon/job system, not the session. | PyInstaller build (120-300s) spawned as session child process. Session evicted after 60s idle → build receives SIGKILL (exit 137). Looked like OOM for weeks. Correct fix: spawn from daemon TaskRunner (not session), or detach from process group. (C031, 2026-05-20) |
| RP38 | **parallel-emitter symptom persistence** | the changeset fixes how a piece of data is produced/filtered at ONE layer, but the SAME data is independently produced/rendered at ANOTHER layer (persist vs stream, backend vs frontend, DB vs live UI, sync vs async path) | a fix applied to one emitter leaves the identical symptom at the parallel emitter. Verify: (1) grep ALL code that produces or renders this data type, not just the path you changed; (2) for each parallel emitter, does it have the same guard/filter? (3) do the layers now DISAGREE (one drops the data, the other still shows it)? The fix must be applied to every emitter, or the filter must live at a shared upstream point. | Empty Opus 4.8 thinking blocks were skipped at the persistence layer (session_unit `_read_formatted_response`), but the Slack streaming path (gateway `thinking_start`) still eagerly wrote a "💭" ghost widget — persisted DB and live Slack stream disagreed. Adversarial review caught it; the persist-layer fix alone was incomplete. Correct fix: lazy-open the widget on first non-empty delta so both layers agree. (run_fd4d756b, 2026-05-30) |
| RP39 | **detached re-exec silent no-op** | the changeset spawns a detached/backgrounded re-exec of a script (`setsid`, `start_new_session=True`, `nohup`, `&`) that re-runs `$0`/the current script with args | the re-exec SILENTLY does nothing unless ALL hold: (1) the script path is ABSOLUTE — a relative `$0` (`./prod.sh`) is unfindable in the detached child's cwd, especially if the spawn runs inside a `cd somewhere` subshell; (2) the interpreter is FIXED and matches the script's shebang — `$SHELL` is the user's login shell (often zsh) and mis-executes a `#!/bin/bash` script with bashisms (`BASH_SOURCE`, `[[ =~ ]]`); (3) the platform supports the detach primitive — `setsid` does NOT exist on macOS (use Python `start_new_session=True`); (4) there is NO result relay back to the original caller IF the op kills that caller (daemon stop/restart reap the originating session) — the outcome must be observable out-of-band, a /tmp relay with no reader is dead code. Verify each by actually running the detached path E2E, not by reading it — a silent no-op passes every unit test because the spawn "succeeds". | C034 prevention re-exec ran `python ... reexec-if-descendant "$SHELL" "$0"` from inside a `cd backend` subshell: detached grandchild inherited cwd=backend/, relative `./prod.sh` not found, AND `$SHELL`=zsh vs `#!/bin/bash` — the prevention did NOTHING (reproduced E2E by adversarial). Fix: resolve `$0` absolute, use `/bin/bash`, fire-and-forget (no relay, caller is reaped). (run_b5592983, 2026-05-30) |
| RP40 | **single-platform compile check misses cross-platform break** | the changeset adds a `#[cfg(target_os=X)]` (or `#[cfg(unix)]`, platform-conditional) function/item, AND verification is a compile-check on ONE platform (`cargo check` on the dev machine, not the CI matrix) | a `#[cfg(target_os="macos")]` fn referenced by an UN-gated caller compiles on macOS but fails name-resolution (Rust E0425) on every other target — Rust type-checks ALL non-cfg-gated items on every target, so an un-gated caller of a gated callee is a guaranteed cross-target build break. `cargo check` on the dev OS passes and hides it. Verify: for each new `#[cfg(...)]` item, either (a) gate ALL its callers identically, or (b — preferred) keep cross-platform-stdlib helpers UN-gated with only the platform-specific lines inside an inner `#[cfg(unix)]`/`#[cfg(windows)]` block (match sibling helpers like `copy_dir_recursive`). If a cross-target toolchain isn't installed locally, flag the gap explicitly for CI — never claim "compiles" from a single-platform check. | `atomic_install` was `#[cfg(target_os="macos")]` but its un-gated caller `install_guardian` referenced it → E0425 on the Windows/Linux build. macOS `cargo check` passed (exit 0); the Windows CI build would have failed. Adversarial caught it. Fix: removed the cfg-gate (cross-platform stdlib, unix bits in inner `#[cfg(unix)]`), matching the un-gated `copy_dir_recursive`. (run_8a9de435, 2026-05-30) |

| RP41 | **non-orthogonal liveness/health discriminator** | the changeset adds logic that decides "is X stuck/dead/unhealthy?" and ACTS on it (kill, interrupt, restart, alert) based on a single signal | verify the signal is ORTHOGONAL to the failure — i.e. it reads DIFFERENTLY for the healthy case vs the failing case. The trap: a signal identical for "working slowly" and "wedged" cannot discriminate between them. (1) name the healthy scenario and the failure scenario explicitly; (2) ask "does the signal differ between them?" — if BOTH produce the same reading (e.g. event-silence during a long tool: silent whether busy or hung), the discriminator is unsound and false-positives on the healthy case; (3) require a SECONDARY signal that IS orthogonal (CPU activity, progress counter, a heartbeat the healthy path emits but the dead path cannot); (4) confirm tests exercise the FAILURE scenario, not just the adjacent healthy one that happens to pass. Bias the ambiguous/unreadable case toward NOT acting (fail-safe). | Tool-hang escape v1 gated a warm interrupt on open-tool-use + event-silence. But `_last_event_time` refreshes per-message only, so a single long tool (Bash build, Agent sub-agent) is event-silent whether genuinely wedged OR perfectly healthy — the signal was NOT orthogonal. It would have interrupted healthy long tools (incl. the adversarial sub-agents reviewing it) and destroyed their work. Tests passed because they validated the THINKING case (thinking_delta DOES refresh the timer), never the tool-execution case. Fix: tree-CPU-delta as the orthogonal secondary signal — a wedged tool burns 0 CPU, a working tool (or its sub-agent CLI child) burns >0. (run_fb6e94a9, 2026-06-23) |
| RP42 | **sample-encoded discriminator (pattern tuned to observed positives only)** | the changeset adds a matcher/classifier/guard whose pattern was derived from a set of OBSERVED positive examples (DB rows, log samples, captured failures) AND acting on a true match has high cost (kill, block, delete, alert) | a discriminator that fits every observed positive can still be encoding the SAMPLE rather than the CLASS. Verify against a NEGATIVE corpus, not just the positives: (1) does the codebase/domain produce legitimate outputs that share the matched surface but are NOT the target? (in a meta-heavy repo, prose/docs/tests that DISCUSS the very syntax being detected); (2) build an explicit non-match test set from those legit cases and confirm 0 false positives; (3) prefer STRUCTURAL signals (multiple orthogonal conditions) over one surface pattern tuned to the samples; (4) for high-false-positive-cost guards, ask "what's the most common legit output that looks closest to a positive?" and test exactly that. DB/sample validation gives false confidence — it proves recall on seen cases, not precision on unseen legit ones. | Tool-call-leak guard v1 keyed on a `call\n<invoke name=` prefix that matched all 7 DB-confirmed leaks — but that shape is EXACTLY what an assistant turn documenting/explaining the leak emits (the bug report itself, the source comment, the test file). It would have wrongfully killed any meta turn discussing the syntax. Adversarial generalization attack caught it. Fix: 3 orthogonal signals — strip fenced/inline code (docs vs raw), match the structural body (`<invoke name="X">` + `<parameter`/`</invoke>`) not the optional prefix, AND require no real ToolUseBlock in the message. (run_e607c4cd, 2026-06-24) |
| RP43 | **destructive op keyed by a non-unique field** | the changeset adds an operation that REMOVES / overwrites / archives / deletes items, and selects WHICH items to act on by matching a field that is not guaranteed unique (title, name, label, message text) | a non-unique key over-matches: a protected/keep item that merely SHARES the key value with a targeted item is destroyed as collateral. For removal, match by STABLE IDENTITY — the tuple the parser/store actually keys on (e.g. (title, section), primary key, line/offset from the same parse), never a single human-readable field. Verify: (1) can two items in the corpus share the matched field? (in DDD docs the same title legitimately appears in different sections — a Pitfall and a Model both named after a subsystem); (2) construct the same-key collision in a test where one item is targeted and the other is protected, assert the protected one SURVIVES and is not silently moved/lost; (3) if the op also archives, confirm collateral items are recoverable — the worst case is "deleted from source AND absent from archive". | reclaim_noise_entries selected noise by (ref0+age+dormant) then _strip_entries matched bullets by TITLE ALONE. A keep-class `[model] **Cache invalidation**` (ref:5) sharing a title with a reclaimed `[pitfall] **Cache invalidation**` (ref:0) was stripped from source AND never archived — unrecoverable loss of the exact protected knowledge the keep-class guard existed to save. 70 green tests missed it (none built the collision). Fix: strip by (title, section) identity, tracking current_section like parse_entries. (run_94fd5597, 2026-06-25) |
| RP44 | **identity-gate keyed by a raw user-supplied name** | the changeset adds a security/privacy/access gate that ALLOWS or DENIES based on matching a caller-supplied string (filename, path, key, id) against an allow/deny set | a name is not an identity. The gate is bypassable by any alias that resolves to the same protected resource but presents a different string: (1) **case** — `memory.md` ≠ `MEMORY.md` as strings but the filesystem (APFS/NTFS) reads the same file; casefold both sides; (2) **path** — `../X`, `./X`, `subdir/../X`, absolute paths all dodge a basename deny-set; `.resolve()` + confine with `is_relative_to(root)`, then check the resolved basename; (3) **symlink** — closed by `.resolve()` (follows to real target name); (4) **hardlink** — a 2nd dir entry for the SAME inode that `.resolve()` CANNOT unmask; close by inode identity `(st_dev, st_ino)` against the protected files. Also: a privacy gate must DEFAULT-DENY (required arg / strictest set, never a permissive default). Verify: build the alias corpus (case/traversal/symlink/hardlink variants) as explicit DENY tests — the happy-path deny test (`gate("MEMORY.md", excluded={MEMORY.md})`) proves nothing about variants. This is GUI01 on a security gate: you wrote the guard AND its test, so the test only covers the variant you imagined. | Reversible Context Recall privacy gate (AC4) compared raw `--file` against `{MEMORY.md,...}`. `recall("memory.md")` and `recall("../MEMORY.md")` both leaked the protected MEMORY.md content (live-confirmed by adversarial). My own AC4 tests covered only the exact-string deny path. Fix: `_basename_key()` (casefold+strip-dir) on both sides, `.resolve()`+`is_relative_to` confinement, inode-identity check for hardlinks, `--session-type required=True`. (run_9de88af9, 2026-06-26) |
| RP45 | **injected-deps test leaves the real adapter uncovered** | a pure-logic function takes its I/O as INJECTED dependencies (fetchers, samplers, callables) for testability, AND a separate thin "wiring" adapter constructs the REAL dependencies and calls it | the unit test that injects fakes proves the LOGIC but exercises ZERO of the real wiring — the adapter that names the real method/field/endpoint is completely uncovered. A typo'd method name, a renamed field, a wrong endpoint path lives precisely there and passes every logic test. Worse, if the wiring failure is swallowed by a fail-safe branch (try/except → benign default), the check is SILENTLY DEAD: it always passes and never fires on the real condition it exists to catch. Verify: for every pure-logic unit with injected deps, require ≥1 test that calls the REAL adapter against the live resource (process, file, endpoint) and asserts a real non-default value (e.g. RSS > 0, not just "didn't crash"). Grep the adapter for the actual external method/field names and confirm they EXIST in the dependency's current API (don't trust memory — APIs rename). | Runtime probe `_fetch_rss` called `resource_monitor.get_system_memory()` (real method is `system_memory()`) reading `process_rss_mb`/`used_mb` (real field is `effective_used`, in bytes). Both wrong → AttributeError swallowed by the probe's fail-safe → the "RSS < 3.5GB" check always passed, structurally incapable of firing. The AC1 test injected a fake `rss_fetcher`, so the real `_fetch_rss` had ZERO coverage. Adversarial found both CRITICALs by reading resource_monitor.py. Fix: `process_tree_rss(os.getpid())` + a test calling the REAL `_fetch_rss` asserting `> 0`. (run_f646b175, 2026-06-26) |
| RP46 | **enum-matched gate misses a sibling severity/status value (fail-open on the un-enumerated tier)** | the changeset adds a gate/filter/router that ACTS (block, kill, alert, drop) when a field matches an ENUMERATED set of values (`severity in {HIGH}`, `status == "failed"`), AND that field's value space is defined in MORE THAN ONE place (specialist schema vs artifact schema vs scoring script) | an enumerated match silently fails-OPEN on any legitimate value NOT in the set — and the most dangerous case is when the MISSING value is MORE severe than the ones you listed. Grep EVERY source that emits the field (schemas, templates, sibling scripts, prompt docs) and build the FULL value vocabulary; a value present in any emitter but absent from your gate set is a hole. Verify: (1) list every distinct value the field takes across ALL emitters (not just the artifact schema you read); (2) for each, does the gate handle it, and is the handling correct for its severity ordering? (3) for a blocking/safety gate, prefer fail-CLOSED on unrecognized-but-non-benign values over an exhaustive allowlist that rots as new values are added. The trap is asymmetric: forgetting a LOW-equivalent value is harmless; forgetting a CRITICAL-equivalent inverts the gate's purpose. | The finding-confidence gate (`_blocked_findings`) blocked `severity in {HIGH, MEDIUM, MED}` but NOT `CRITICAL` — yet `critical` is a live severity in 3 review-agent schemas + `confidence_score.py:133` (which treats critical==high). The single MOST severe finding class was the one class the gate let through — fail-open inverse of intent. My own 11 tests never covered CRITICAL (I enumerated the values I remembered). Adversarial grepped the sibling schemas and found it. Fix: block `{HIGH, CRITICAL}` confidence-independent + explicit CRITICAL test. (run_7583af5f, 2026-07-01) |
| RP47 | **test-theater — a test that cannot fail on the bug it guards** (grep-able tell) | the changeset ADDS or MODIFIES a test whose stated purpose is to guard a specific behavior/fix — especially a "regression test", a test whose docstring claims "drives the REAL path / no mock", or a test written by the same agent that wrote the code-under-change | a self-authored green test gives false confidence identical to a real one — the test must be able to go RED when the guarded behavior breaks. Two mechanical tells, either one = finding: **(1) mock-of-the-subject** — grep the test for `patch(`/`monkeypatch`/`Mock(` targeting the EXACT symbol the changeset modifies (`grep -n "patch(.*<symbol_under_change>" <test>`); if the test patches out the function it claims to verify, it tests the mock, not the code. This is a CANDIDATE signal — confirm with the revert-verdict before filing; a mock of a *collaborator* (not the subject) is legitimate boundary-mocking (RP45 territory), not theater. **(2) local re-derivation** — the test recomputes a prod formula/constant in a local helper and asserts the formula against its own copy (reverting the prod line leaves the test GREEN). Verify the test either IMPORTS+CALLS the prod symbol (single source) or is **mutation-proven**: revert the exact prod line the test guards → the test MUST go RED; restore → GREEN. A test that stays green on revert is theater — reject it. Prefer pointing the test at ONE prod source over re-deriving. (Mechanical form of correctness.md §7 "would the test pass even if broken?"; test-theater twin of RP31 synthetic-data.) | `TestInitTimeoutResumeFloor` defined a LOCAL `_init_timeout()` recomputing the resume-floor formula, then asserted against its own copy — reverting the prod line left all 3 tests GREEN (zero protection), and its docstring claimed it guarded the behavior. Separately, GS_RCHAIN_* probes `patch`ed out `_hybrid_section_scores` — the very function under change — validating the mock, not the rewrite; docstring literally said "no mocked scorer". Both caught only by a Gate-2 mutation probe (revert → still green = theater). Fix: extract prod to a single source + point the test there, or mutation-prove RED-on-revert. (run_4b74b764, run_1e2e663b) |
| RP48 | **stale/false comment — behavior changed, the comment that names the mechanism did not** (lying comment) | the changeset MODIFIES a function/module body whose docstring or a neighboring comment NAMES the mechanism, value, or contract being changed (e.g. "hybrid 0.6v+0.4k", "single-writer", "passes True", "not yet built", a specific constant/formula, "BOTH legs") | code is truth, comments are hypotheses — a comment describing the OLD behavior is now a legibility-decay bug that actively misleads the next reader (and the next agent). **Grep-decidable trigger (narrows the candidate set mechanically):** for each token the DIFF changed (a renamed symbol, a changed constant/number, a flipped branch), grep it against docstring + ±50 lines of comment lines; **flag = the changed token appears in a comment/docstring line that is NOT itself in the diff** (i.e. the behavior moved but the comment describing it stayed). Then adjudicate ONLY the flagged lines: does the unchanged comment still describe the pre-change behavior? If yes → correct or delete it in the SAME diff, never left because it "looks roughly right". **Scope-out (avoids false positives):** N/A if the named mechanism/value is UNCHANGED by this diff — a comment "hybrid 0.6v+0.4k" on a function whose weighting didn't change is still true; only a diff that changed that value flags it. Also treat a comment asserting deployment/build state ("not yet built", "TODO wire this") as suspect: verify against code, don't trust the comment. This is R7's docstring-co-update made a review assertion, not optional. | R7 (AGENT.md) is the authority — it names this exact class and even lists the same tell-mechanisms ("hybrid/vector/passes True/single-writer/BOTH legs"). A comment naming a mechanism whose behavior changed has misled repeatedly: C043 (stale-log-string → live-cause), PIT43 (comment-drift), PIT17/18/31/66, GUI117, O012 ("comments are hypotheses"); a lying "not yet built" comment also *contributed* to a runtime-state misread (C038-subfinding, though C038's primary class is deploy-state inference, not comments). A green suite never catches it — a comment can't fail a test. Fix: correct or delete the comment in the same commit as the behavior change; adversarial MUST ask "does this diff leave any docstring/comment contradicting the new behavior?" |
| RP49 | **security/privacy redaction written as a denylist (enumerate-to-REMOVE) instead of an allowlist (enumerate-to-KEEP)** | the changeset adds/modifies a function that STRIPS or redacts fields before crossing a security/privacy boundary (an API response, a public projection, a cross-user surface) by ENUMERATING the sensitive fields to remove | a denylist fails-OPEN on schema growth: every field ADDED to the object later is exposed by default until someone remembers to add it to the strip-list — the leak is silent and grows with the codebase. A privacy boundary must fail-CLOSED: enumerate the fields that are SAFE TO KEEP (allowlist) and drop everything else, so an unanticipated new field is withheld, never leaked. Mechanical tell: the redaction builds `{k: v for k,v in obj if k not in SENSITIVE}` / `del obj[x]` / `obj.pop(x)` per sensitive key → invert to `{k: obj[k] for k in ALLOWED}`. Also enumerate EVERY endpoint that returns the object — the LIST projection and the DETAIL projection are independent leak surfaces (guarding one misses the other). Distinct from RP44 (identity-gate keyed by a raw NAME — aliasing bypass) and RP46 (enumerated MATCH fails-open on an un-listed value); RP49 is enumerate-to-REMOVE fails-open on an un-listed field ADDED LATER. | The eval golden-set drawer redacted 6 sensitive fields of private cases via a denylist in `get_case_detail`; adversarial found it leaked TWICE through the same list — `expected_response_contains` (the answer keywords) was never added to the deny-set, and `source` leaked via the SEPARATE `get_golden_set` LIST projection. The denylist felt complete to its author. Fix: invert `get_case_detail` to an allowlist-of-metadata (fail closed), and audit every endpoint returning the object. (run_1f588e53) |
| RP50 | **security/verification gate that fails OPEN — a check that silently passes when it cannot actually run** | the changeset adds a GATE that decides pass/block (a security scan, a baseline diff, a version/coverage assertion, any "block if X" check) | every way the gate can fail to actually inspect its target MUST fail CLOSED (non-zero/block), never silent-pass. Enumerate: (a) **empty input** — 0 files/rows scanned reads as "clean"; (b) **missing target** — the scan dir/table doesn't exist → early-return empty → PASS; (c) **tool crash / non-JSON** — parse fails → treat as pass; (d) **version/plugin drift** — a stale analyzer under-detects but exits 0; (e) **a self-check that can't fire** — e.g. `getattr(mod,'VERSION')` is None so the guard's `if ver and ...` never triggers (dead code). For EACH: assert positive evidence the gate ran (files-scanned>0, plugins non-empty, version==pin) and `_die`/exit-nonzero otherwise. Also: a BASELINE-diff gate must fingerprint at the individual-finding level, never delegate pass/fail to a tool's own `--baseline` flag if that flag suppresses per-file/per-type (a 2nd same-type finding in a baselined file then passes silently — a false-green). And a content-only fingerprint collides two identical-text findings — add an occurrence-ordinal. | A code-security gate (`security_scan.py`): `bandit -b` silently passed a 2nd md5 in a baselined file (Gate-1); the fingerprint collided two identical-text findings so baselining one absorbed the other (Gate-2 CRITICAL, mutation-verified); `detect_secrets.VERSION` was None so the version guard was dead code (Gate-2 CRITICAL); detect-secrets returned 0 findings on an untracked tree AND on an absolute path AND when the scan dir was missing (fail-opens). ALL fixed to fail closed + finding-level diff + occurrence-ordinal. A gate that can't run must never read as "passed." (run_4b007e00) |
| RP51 | **producer/validator allowlist drift — a validator rejects a value its OWN producer legitimately emits** | the changeset adds a new value a PRODUCER writes into a shared artifact (a new `kind`/`type`/`status`/`category` enum member) OR adds/tightens a VALIDATOR with a closed allowlist for such a field | a closed-allowlist validator and its producer are two ends of one contract — if the producer gains a value the validator's allowlist wasn't updated for, the validator fail-CLOSED-REJECTS VALID data (blocks legitimate writes / breaks regeneration), the mirror of RP46's fail-open. Mechanical tell: grep EVERY site that writes the field (`grep -n '"kind":' producer.py exporter.py`) and diff that value-set against the validator's allowlist constant (`_VALID_*`/`_ALLOWED`); any value emitted-but-not-allowed = finding. Applies BOTH directions: adding a producer value → update the validator allowlist in the SAME change; adding/tightening a validator → enumerate all real producers first. Verify non-vacuously: a doc carrying the real producer's output must PASS the validator (revert the allowlist entry → RED). Distinct from RP46 (gate ACTS on an enumerated match, fails-OPEN on a missing value) — RP51 is a validator that REJECTS on a missing-from-allowlist value its producer really writes (fails-CLOSED on valid data). | `_COVERAGE_HOLE_KINDS = {"route","file","repo","query"}` rejected `kind="gitignored"` which `parser.py:1496` had started emitting (same-day gitignore work); `finalize_v3` blocked on the live `code-intel.json`, breaking domain-layer regeneration. Adversarial audited all emitted kinds across parser.py + json_exporter.py vs the allowlist. Fix: add `"gitignored"` + RED-on-revert test. (run_89e28075, 2026-07-18) |
| RP52 | **identity/principal read from the REQUEST (body/header/query) instead of the server-VERIFIED token — trust-the-request, not the principal** | the changeset has a handler/endpoint that reads a user identifier (`user_alias`, `login`, `creator`, `user_id`, `sub`, `owner`) from the request body/header/query, OR decodes a JWT without signature verification, OR exposes an endpoint with no auth middleware, OR treats a SigV4 IAM role / corporate-network reachability as the business identity | authN ≠ authZ ≠ network reach. The **verified principal** (what the auth middleware actually authenticated) must be propagated so the handler is *forced* to use it; whoever needs the alias reaches for `body.user_alias` because it is the easiest thing on hand, and the request is attacker-controlled. Mechanical tell: grep the handler for `body.get("user_alias"\|"login"\|"creator"\|"user_id")`, `request.headers[...user...]`, `X-Impersonate-User`, `jwt.decode(..., verify=False)` / `options={"verify_signature": False}`, or a base64-decode of a JWT payload with no signature check. **Flag only when** an identity field sourced from the request is used AS the authorization principal while a verified-principal object exists (or should) in the same call chain — NOT any body field that merely happens to be a name (that scoping avoids false-positives on legitimate form data). The correct sibling often already exists in-repo (an `authenticate()`-derived `UserContext`) but the vulnerable path doesn't use it. Paired red-team assertion (the guard is real only if this can go RED): **"attacker sets `user_alias=<victim>` in the body/header while authenticated as themselves → expect 403 / the server uses the verified principal and ignores the request-supplied value."** Distinct from RP44 (identity-gate KEYED on a raw name — aliasing bypass *at the gate*) and RP49 (denylist redaction) — RP52 is the identity **SOURCE** being the untrusted request, *upstream* of any gate. | The 2026-06-26 Cataphract sweep (`Knowledge/Library/security-coe-lessons-cataphract-sweep.md`) — the single most-recurring class across 4 sibling-team COEs: 408242/DAT (4 tickets — `user_alias`/`login`/`creator` in body silently overrides the JWT subject; JWT accepted without signature verify; a credential-return endpoint handed the victim's Federate id_token to any authenticated caller), 408244/CCT (5 — server trusts client-asserted identity, incl. a WebSocket handler), 408241 (FightBot gateway base64-decoded the JWT payload with no signature verify + honoured `X-Impersonate-User` in prod), 408243 (cross-tenant IDOR from caller-supplied IDs). Fix pattern: derive identity from `authenticate()` → a `UserContext` the handler must consume; never read identity from the request. |
| RP53 | **sync blocking I/O on the async event loop — a `def` (not `async def`) doing `sqlite3.connect`/`requests.get`/`open().read()`/`time.sleep`/`subprocess.run` called un-offloaded from an async handler/hook** | the changeset adds or calls a SYNCHRONOUS function that does blocking I/O (raw `sqlite3.connect`, `requests`, a big file read, `time.sleep`, blocking `subprocess`) from inside an `async def` WITHOUT `await asyncio.to_thread(...)` / a pooled async client — especially on a hot path (a polled endpoint, a per-request loop, a per-session iteration) | a single-threaded asyncio daemon has ONE event loop; a sync blocking call on it freezes EVERY coroutine — including `/health` — for the call's duration. This is a DISTINCT starvation cause from thread-pool exhaustion (RP35): RP35 is "too many threads dispatched to a shared executor"; RP53 is "no thread at all — the work runs ON the loop." Both make a liveness/health endpoint miss its watchdog deadline and can present as a false "backend down / offline" while the process is alive. Mechanical tell: grep changed + called files for `sqlite3.connect`, `requests.`, `urllib.request`, `time.sleep`, `subprocess.run`/`.check_output`, `open(...).read()` on a large file — then check the CALLER: is it an `async def`? is `asyncio.to_thread`/`run_in_executor` in the call chain? If a sync blocking call is reachable from an async handler with no offload → finding. Worse on a POLLED path (frontend polls every N s) or a per-item loop (once per session/row). Fix: make the function async + use the async client/pool, OR wrap the call site in `await asyncio.to_thread(fn, ...)`. Scope-out: sync blocking I/O that runs ONLY in a background thread / subprocess / job worker (never on the main loop) is fine — the bug is specifically sync-on-the-loop. | `count_pending()` was a sync `sqlite3.connect()` called un-offloaded inside the async `/sessions/streaming-state` endpoint (chat.py:871), which the frontend polls ~every 15s, once PER live session. It blocked the daemon's event loop for the connect+COUNT; when the DB was briefly locked (WAL checkpoint / git-sync snapshot) the loop stalled and `/health` with it → the Rust watchdog (3s timeout) fired `backend-terminated-restarting` → UI showed "Backend is unavailable" + disabled all inputs, though the daemon was alive. This was a SECOND independent cause of the same offline flap the connection-pool fix (RP35 class) targeted — the pool alone would have left it live. Meta-review (not adversarial code review) caught it by asking "what OTHER path produces this symptom?". Fix: `count_pending` → async + pooled read borrow; caller + 19 test sites → await. (run_7e8a2030, 2026-07-28) |
| RP54 | **row-`LIMIT` on a query whose rows are GROUPED + RE-SCORED after the fetch — the cap silently changes which GROUPS surface unless the group score is PRIMARY-keyed on the same column the `LIMIT` orders by** | the changeset adds/changes a `LIMIT N` (or top-N cap) on a query whose result ROWS are then grouped (by session, user, key) and the GROUPS ranked by a score computed IN CODE from the grouped rows (density, recency, richness, count, avg) | a `LIMIT N ORDER BY col` returns the global top-N ROWS by `col` — but if the downstream GROUP score depends on the FULL set of a group's rows (e.g. count/density/recency-of-newest across ALL matches), truncating rows at N changes each group's computed score, so the surfaced groups shift with N (non-deterministic w.r.t. the cap). The cap is quality-preserving ONLY when the group's ranking key is PRIMARY on the same `col` the LIMIT orders by (e.g. group keyed by MIN(col) = the best row it owns, which top-N-by-col is guaranteed to keep). Mechanical tell: a `LIMIT` on a query feeding a Python `groupby`/`defaultdict` + a `sorted(key=<score over the grouped rows>)` where the score is NOT dominated by the LIMIT's ORDER BY column → finding. Verify: does `top_groups(LIMIT=small)` == `top_groups(LIMIT=∞)` on a corpus larger than the cap? If not, either (a) make the group score primary-keyed on the ordered column, or (b) don't LIMIT (aggregate in SQL). A secondary signal (recency/density) may survive ONLY as a TIEBREAK after the primary key, never as a weighted term. | session_recall.search() added `LIMIT 500` to `... ORDER BY fts.rank` while grouping matched messages by session and scoring sessions `density*0.4+recency*0.35+richness*0.25` — the naive fix. Measured: full-scan top-5 sessions ≠ LIMIT-500 top-5 (grouping happens after the row-cap). Gate-0 skeptic caught it pre-code. Fix: made session scoring rank-primary (`min fts.rank` wins, density tiebreak) — under which LIMIT-500 top-N == full-scan top-N (verified stable N=200..1000), because the deciding message is the global-best-rank one the LIMIT always keeps. Recency was DROPPED (a weighted recency term reintroduces the full-row-set dependency that breaks the guarantee — a forced tradeoff, not an oversight). Distinct from RP53 (sync-on-loop) — same subsystem, different bug class: RP53 is HOW the query runs, RP54 is WHETHER a cap corrupts grouped results. (run_78bd708f, 2026-07-28) |
| RP55 | **verification check treats "the check couldn't run" as a NEGATIVE result — absence-of-evidence collapsed into evidence-of-absence, reintroducing the false-positive the check was added to prevent** | the changeset adds a liveness/existence/health VERIFICATION (a subprocess like `launchctl`/`ps`/`pgrep`, an API probe, a file-exists check) whose result GATES a consequential action (declare dead, block, alert, kill), AND the check has a failure mode distinct from its two real answers — the tool itself errored / timed out / returned unparseable output | a boolean verify (`present: bool`) has THREE real outcomes, not two: **present** (evidence of alive), **absent** (evidence of gone), and **couldn't-check** (the tool erred — NO evidence either way). Collapsing couldn't-check into `false`/absent means a transient tool hiccup reads as "the thing is gone" → the exact false-positive the verification was meant to KILL comes back through the verification itself. This is the inverse of RP50 (fail-OPEN): here the check fails to a false-POSITIVE (wrongly acts). Mechanical tell: a verify fn ending `Err(_) => false` / `except: return False` / `catch { return false }` whose `false` then drives a destructive/user-visible action. Fix: make it TRI-STATE ({Present|Absent|Unknown}) and map Unknown to the SAFE side (assume-alive / don't-act / degrade, not terminate) — evidence-of-absence must be a DEFINITE negative, never a tool failure. Distinct from RP41 (a signal that can't discriminate healthy-vs-failing) and RP50 (a gate that silently passes when it can't run): RP55 is a check that silently ACTS-destructively when it can't run. | The false-offline root-fix ADDED a liveness check (`daemon_pid_present` → `launchctl print`) to gate "declare the daemon dead." Its `Err(_) => false` treated a launchctl COMMAND failure as "process gone" → a transient launchctl hiccup during a system-load stall would false-declare death and disable the UI — the very bug the fix targeted, reintroduced via the fix's own verification. Gate-2 operational specialist caught it. Fix: tri-state `daemon_liveness() {Alive|Gone|Unknown}`; the watchdog maps `Unknown` (and `Alive`) to "not dead" (stay degraded), only a definitive `Gone` declares death. (run_13094a88, 2026-07-28) |

## Output Format

For each applicable pattern, one line:
```
RP1: pass -- proc.kill() in timeout handler + finally (voice_transcribe.py:92,112)
RP3: pass -- useEffect cleanup releases stream + recorder (useVoiceRecorder.ts:58-68)
RP5: N/A -- no FormData in this changeset
```

## Maintenance

The REFLECT stage is responsible for maintaining this file. When post-pipeline review (E2E, external, or user feedback) finds bugs the pipeline missed:

1. If the bug fits an existing RP pattern -- investigate why the checklist missed it and tighten the verification criteria above.
2. If the bug is a new pattern -- **append a new row** (RP30, RP31, etc.) to the table above. Include: trigger condition, what to verify, and the real bug as the example.
3. Update the pattern count in `backend/skills/s_autonomous-pipeline/stages/review.md` check #6 if the total changes.

This ensures the pipeline learns from every review cycle. Without this step, lessons live in IMPROVEMENT.md but never reach the checklist that would prevent recurrence.
