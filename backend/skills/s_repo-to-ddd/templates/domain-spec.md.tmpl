# Domain Specification — {{DOMAIN_NAME}}

> **Language-neutral implementation specification.** Reconstructed statically from
> the codebase (no build, no run). Every business rule is anchored to a source
> location. This document describes WHAT the domain does and WHY, in terms
> reimplementable in ANY language/stack — it does NOT prescribe the source language.
> Legacy-specific mechanics appear only in §7 as migration notes, never as
> requirements.
>
> **Domain provenance:** structural cluster `{{DOMAIN_ID}}` from code-intel
> `graph_clusters` — {{SYMBOL_COUNT}} symbols / {{DATA_OBJECT_COUNT}} data objects,
> cohesion {{COHESION}}. Languages: {{LANGUAGES}}.
> **Source files:** {{FILES}}.
>
> ⚙️ **How to fill this template (LLM prose layer):** the deterministic manifest
> (`code-intel.json > domain_rules`) supplies every rule_id, its source_symbol,
> operation, target_data_object, disposition, engine_status, and anchor{file,line}.
> Your job is ONLY to write the behavioral prose (§1-§7) FROM those rules — you may
> NOT invent a rule that has no manifest entry, and every manifest rule_id for this
> domain MUST be referenced in §1-§8 (a rule that appears only in the §9 matrix is
> treated as UNCOVERED by `verify_traceability`, fail-closed).

---

## 1. Domain Purpose & Boundary
<!-- One paragraph: what business capability this domain owns, and where its edges
are (what it consumes from other domains vs what it owns). Derive "owns" from the
data objects it WRITES (manifest operation=CREATE/INSERT/UPDATE), "consumes" from
what it reads but does not write. -->

## 2. Ubiquitous Language (glossary)
<!-- Table of the domain's terms → meaning → source anchor. Seed from data-object
names + recurring identifiers in the rules. -->

| Term | Meaning | Source anchor |
|------|---------|---------------|
| {{TERM}} | {{MEANING}} | {{FILE}}:{{LINE}} |

## 3. Inputs & Outputs
<!-- Inputs = data objects read but not written by this domain. Outputs = data
objects written (from manifest), grouped by disposition where present. -->

## 4. Core Business Rules
<!-- ONE entry per manifest rule for this domain. For each: the rule_id, the
invariant in language-neutral terms, its disposition (if any), engine_status, and
the anchor. A rule whose engine_status=suspected_deprecated MUST still be listed
here (never omit a rule — false-100% red line) but flagged. -->

**{{RULE_ID}} — {{ONE_LINE_NAME}}.**
{{LANGUAGE_NEUTRAL_INVARIANT}}
`disposition: {{DISPOSITION}}` · `engine_status: {{ENGINE_STATUS}}` · anchor `{{FILE}}:{{LINE}}`.

## 5. Workflows
<!-- Ordered steps that chain the rules (e.g. build snapshot → merge → classify →
remediate). Cite the rule_ids each step realizes. -->

## 6. Cross-Cutting Concerns
<!-- Error handling, metrics/counters, export/handoff, logging — whatever the domain
does around the core rules. Flag correctness risks (e.g. silent error-swallow) as
migration notes. -->

## 7. Legacy Mechanics (migration notes — NOT requirements)
<!-- Source-language idioms that a rewrite should NOT carry (dynamic DDL as compute,
outer-join operators, vendor functions). This is the ONLY section allowed to name
the source language. A target ('<dynamic:unresolved>' in the manifest) whose name is
computed at runtime is documented here as "writes a runtime-computed target". -->

## 8. Coverage & Known Gaps
<!-- What was extracted vs deliberately not (out-of-scope: deep predicates, runtime
eval). List any rule whose target_data_object is <dynamic:unresolved> and why. -->

---

## 9. Traceability Matrix

> ⚠️ **EXCLUDED from the coverage gate by design.** A rule that appears ONLY here
> (and not in a behavioral chapter §1–8) is NOT considered covered
> (`verify_traceability` mirrors AWS Transform's §1–8-only scan). Fill this from the
> manifest; it is a cross-index, not a substitute for documenting the rule above.

| Rule ID | Disposition | Engine | Behavioral ref (must be §1–8) | Source anchor |
|---------|-------------|--------|-------------------------------|---------------|
| {{RULE_ID}} | {{DISPOSITION}} | {{ENGINE_STATUS}} | §{{SECTION}} | {{FILE}}:{{LINE}} |

**Coverage:** {{COVERED}}/{{TOTAL}} rules referenced in §1–8 → **{{PCT}}%**
(must be 100% — `verify_traceability` fails closed otherwise).

---
*Generated by SwarmAI ai_ready_repo — static, no build/run. Domain manifest =
code-intel.json > domain_rules. Prose is the ONLY LLM-authored layer; structure +
anchors are deterministic.*
