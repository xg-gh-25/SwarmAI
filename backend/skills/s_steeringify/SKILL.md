---
name: steeringify
description: >
  DEPRECATED — absorbed into s_self-evolution PROMOTE operation (2026-05-19).
  Use self-evolution instead. This skill redirects to the PROMOTE operation
  of the governance lifecycle manager.
  TRIGGER: "steeringify", "extract steering rules", "propose rules from corrections".
  REDIRECT: Use s_self-evolution PROMOTE operation instead.
tier: lazy
platform: all
---
# Steeringify — DEPRECATED

**This skill has been absorbed into `s_self-evolution` as the PROMOTE operation.**

The Three-Layer Governance model unifies correction capture, pattern detection,
and rule promotion into a single lifecycle manager.

## What to do instead

When you want to extract recurring patterns into rules:
1. Read EVOLUTION.md corrections
2. Identify 3x threshold patterns
3. Use s_self-evolution PROMOTE operation (includes CLASSIFY + budget check)

The PROMOTE operation does everything steeringify did, plus:
- Intake classification (layer, parent principle, conflict check)
- Budget enforcement (cap check before writing)
- Bias-class awareness (promotes by pattern, not just frequency)
- Candidate queue for sub-threshold proposals

