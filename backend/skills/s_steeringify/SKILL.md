---
name: steeringify
description: >
  Mine EVOLUTION.md corrections for recurring patterns and propose them as
  STEERING.md standing rules. Closes the self-evolution → self-context loop:
  corrections that keep happening become preventive rules automatically.
  TRIGGER: "steeringify", "extract steering rules", "propose rules from corrections",
  "what patterns should become rules".
  DO NOT USE: for one-off rule creation (edit STEERING.md directly), or for
  memory management (use save-memory).
  SIBLINGS: save-memory = facts → MEMORY.md | self-evolution = corrections →
  EVOLUTION.md | steeringify = recurring corrections → STEERING.md rules.
tier: lazy
platform: all
---
# Steeringify

Extract recurring correction patterns from EVOLUTION.md and propose them as
STEERING.md standing rules.

Read INSTRUCTIONS.md for the full workflow.
