---
name: workspace-organizer
description: "Batch file operations for SwarmWS: organize, archive, clean up, and restructure workspace contents. Always previews before executing. Uses mv (not rm) for safety.\n  TRIGGER: \"organize files\"\
  , \"clean up workspace\", \"archive old files\", \"move files\".\n  NOT FOR: explorer, explorer, workspace-git use cases."
tier: lazy
---
# Workspace Organizer

> This skill loads full instructions on activation. Read INSTRUCTIONS.md before proceeding.

TRIGGER: "organize files", "clean up workspace", "archive old files", "move files", "restructure",
DO NOT USE: for single file rename/move (use explorer context menu), deleting files (use explorer delete),
