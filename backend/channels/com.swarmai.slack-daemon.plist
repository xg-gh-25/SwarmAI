<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.swarmai.slack-daemon</string>

    <key>ProgramArguments</key>
    <array>
        <string>__WRAPPER_PATH__</string>
    </array>

    <!-- WorkingDirectory set by wrapper script (cd $BACKEND_DIR) -->

    <!-- Start on login and restart on crash -->
    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <!-- Background priority — won't interfere with user apps -->
    <key>ProcessType</key>
    <string>Background</string>

    <!-- Throttle restarts: wait 10s between launches -->
    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>StandardOutPath</key>
    <string>__LOG_DIR__/slack-daemon-stdout.log</string>

    <key>StandardErrorPath</key>
    <string>__LOG_DIR__/slack-daemon-stderr.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/opt/homebrew/bin</string>
    </dict>
</dict>
</plist>
